# 업로드 · 삭제 목록 (2026-07-30)

로컬 `D:\00work\260729-new\` · 빌드 `D:\00work\ocr-output-260723\06\` · 서버 웹루트 `/www`

**순서를 지킵니다.** 코드가 새 컬럼을 쓰므로 DB 먼저입니다.

---

## 0단계 — 먼저 이것만 (관리자 화면이 막혀 있습니다 · 1분)

| 로컬 | 서버 |
|---|---|
| `web\extend\10_exam.php` | `/www/extend/10_exam.php` |
| `web\exam\assets\gnuboard-skin.css` | `/www/exam/assets/gnuboard-skin.css` |

관리자 좌측 사이드바 2차 메뉴가 **흰 배경에 흰 글자**로 안 보이던 원인입니다.
우리 `gnuboard-skin.css` 가 `/adm/` 에도 주입되어 코어 관리자의 `#gnb` 를 덮었습니다.

☐ `/adm/` → 좌측 메뉴 호버 → **2차 메뉴 글자가 보이는가**
☐ `/adm/` 상단에 우리 네이비 헤더가 **섞이지 않는가**

---

## 1단계 — DB 마이그레이션 (5분)

### ① 파일 2개 업로드

| 로컬 | 서버 | 비고 |
|---|---|---|
| `web\exam\sql\migrate-001-multipd.sql` | `/www/exam/sql/migrate-001-multipd.sql` | |
| `_probe\exam_migrate.php` | `/www/exam_migrate.php` | **웹루트 최상위** (`common.php` 와 같은 자리) |

### ② 실행

1. **최고관리자로 로그인** — `https://axexam.mycafe24.com/bbs/login.php`
2. `https://axexam.mycafe24.com/exam_migrate.php` 접속 → 실행 전 상태 확인
3. **[마이그레이션 실행]** 클릭
4. 검증 7개가 모두 초록이어야 합니다:

```
(1) pd_id 컬럼          5 / 5
(2) pd_id 빈 행 합계     0
(3) uq_month            (mb_id, pd_id, lot_src, lot_period) · Non_unique=0   ★ 가장 중요
(4) 품목별 과정          sqld 3 · bdae-w 3
(5) 고아 주문            0건
(6) ex_qna 신규 컬럼     3 / 3
(7) g5_member 조인       성공
```

> **(3)이 3컬럼이면 멈춥니다.** 두 문제집을 수강하는 회원의 같은 달 두 번째 포인트 지급이
> DB 차원에서 거부됩니다. 에러가 안 나서 나중에 원인을 찾기 매우 어렵습니다.

### ③ 삭제

☐ **`/www/exam_migrate.php` 를 지웁니다.** (`/www/exam/sql/` 은 `.htaccess` 가 막고 있어 남겨도 됩니다)

---

## 2단계 — 코드 업로드

### 신규 (없는 파일이라 그냥 올립니다)

| 로컬 | 서버 |
|---|---|
| `web\exam\check.php` | `/www/exam/check.php` |
| `web\exam\api\board.php` | `/www/exam/api/board.php` |
| `web\exam\api\lib\credit.php` | `/www/exam/api/lib/credit.php` ← **`lib` 폴더를 새로 만듭니다** |
| `web\exam\assets\check.css` | `/www/exam/assets/check.css` |
| `web\exam\assets\check.js` | `/www/exam/assets/check.js` |
| `web\adm\exam_orders.php` | `/www/adm/exam_orders.php` |
| `web\adm\exam_credit_grant.php` | `/www/adm/exam_credit_grant.php` |
| `web\adm\exam_qna_list.php` | `/www/adm/exam_qna_list.php` |
| `web\adm\exam_qna_form.php` | `/www/adm/exam_qna_form.php` |
| `web\adm\exam_board_sync.php` | `/www/adm/exam_board_sync.php` |

### 덮어쓰기

| 로컬 | 서버 |
|---|---|
| `web\exam\buy.php` | `/www/exam/buy.php` |
| `web\exam\mypage.php` | `/www/exam/mypage.php` |
| `web\exam\api\me.php` | `/www/exam/api/me.php` |
| `web\exam\api\qna.php` | `/www/exam/api/qna.php` |
| `web\exam\api\problems.php` | `/www/exam/api/problems.php` |
| `web\exam\api\products.php` | `/www/exam/api/products.php` |
| `web\exam\assets\mypage.css` | `/www/exam/assets/mypage.css` |
| `web\exam\assets\mypage.js` | `/www/exam/assets/mypage.js` |
| `web\theme\axexam\head.php` | `/www/theme/axexam/head.php` |
| `web\theme\axexam\tail.php` | `/www/theme/axexam/tail.php` |

### ⚠ 맨 마지막에 올립니다

| 로컬 | 서버 |
|---|---|
| `web\adm\admin.menu600.exam.php` | `/www/adm/admin.menu600.exam.php` |

`adm/admin.lib.php` 가 **모든 관리자 페이지에서** 이 파일을 include 합니다.
위의 관리자 화면 5개를 먼저 올려 주소로 직접 열어 확인한 뒤 이걸 올립니다.
문법 오류가 있으면 관리자 전체가 죽습니다.

**검증** — 메뉴 파일을 올리기 전에 주소창에 직접 넣습니다(최고관리자는 메뉴 등록 없이 통과합니다):

```
/adm/exam_orders.php        수강 신청
/adm/exam_credit_grant.php  포인트 지급
/adm/exam_qna_list.php      질문 검수
/adm/exam_board_sync.php    과목게시판
```

화면이 뜨면 정상 → 메뉴를 올립니다. 백지·500 이면 **그 파일만 지웁니다.** 관리자는 살아 있습니다.

---

## 3단계 — 빌드 산출물 (`06\` → `/www/exam/`)

### 올립니다

| 로컬 (`D:\00work\ocr-output-260723\06\`) | 서버 | 크기 |
|---|---|---|
| `brand.php` | `/www/exam/brand.php` | 332B — **신규** |
| `index.html` | `/www/exam/index.html` | 32KB |
| `detail.html` | `/www/exam/detail.html` | 11KB — **신규** |
| `check.html` | `/www/exam/check.html` | 157B — `check.php` 로 리다이렉트 |
| `sqld.html` | `/www/exam/sqld.html` | 151B — `detail.html?pd=sqld` 로 리다이렉트 |
| `assets\` | `/www/exam/assets/` | **덮어쓰기만.** 폴더를 지우고 올리지 마십시오 |
| `pd\sqld\` | `/www/exam/pd/sqld/` | **신규 폴더.** 아래 참조 |

`pd\sqld\` 안에 올릴 것 (**`problems.json` 은 제외**):

```
theory.js            211B
theory_content.js    80KB      ← 이론은 DB 가 아니라 여기 구워져 있습니다
videos.js            21B
figs\                205KB (SVG 58)
theory\              160KB
```

> ⚠ **`assets\` 는 덮어쓰기입니다.** 서버의 `/www/exam/assets/` 에는 빌드에 없는
> `gnuboard-skin.css` · `mypage.css` · `mypage.js` 가 함께 있습니다.
> FileZilla 로 폴더를 지우고 올리면 그 셋이 사라집니다.

> ⚠ **`pd\sqld\problems.js` 는 올리지 않습니다.** 서버에서는 `api/problems.php` 가 이기고,
> 정답이 박힌 388KB 를 매번 내려보낼 이유가 없습니다.

### 🔴 서버에서 지웁니다 (FTP 업로드는 지워주지 않습니다)

정적 데이터가 `pd/<문제집>/` 로 내려갔습니다. **옛 납작 사본을 지우지 않으면**
정답이 박힌 옛 `problems.js` 가 계속 공개 조회되고, 상대경로로 떨어지는 버그가
조용히 낡은 데이터를 읽습니다.

| 서버에서 삭제 |
|---|
| ☐ `/www/exam/problems.js` |
| ☐ `/www/exam/videos.js` |
| ☐ `/www/exam/theory.js` |
| ☐ `/www/exam/theory_content.js` |
| ☐ `/www/exam/figs/` (폴더 전체) |
| ☐ `/www/exam/theory/` (폴더 전체) |
| ☐ `/www/exam/problems.json` (있으면) |
| ☐ `/www/exam/videos/` (mp4 가 올라가 있으면 — 411MB) |

> 로컬 `06\` 의 같은 파일들은 이미 `--prune` 이 정리했습니다.

---

## 4단계 — 관리자 작업

### ① 문제 재임포트

`/adm/exam_import.php` → **`D:\00work\ocr-output-260723\06\pd\sqld\problems.json`**
(경로가 바뀌었습니다 — 예전 `06\problems.json` 이 아닙니다)

기대값: **신규 0 · 갱신 0 · 변경없음 300 · 회차 6행**

DB 회차 라벨이 `자사 모의고사 01회` → `모의고사 01회` 로 바뀝니다.

### ② 과목게시판 만들기 (신규 절차)

1. `/adm/board_list.php` → **게시판 추가**
2. 테이블명(`bo_table`)을 **`sqld_sj`** 로 (`/adm/exam_board_sync.php` 표에 적힌 값 그대로)
3. 제목 `SQLD 과목게시판`. 그룹·스킨은 공지 게시판과 같게
4. **읽기 권한은 비회원까지 열어둡니다** — 쌓인 공개 Q&A 가 신규 방문자에게 보여야 수강 신청으로 이어집니다
5. 저장 → `/adm/exam_board_sync.php` 에서 **[말머리 맞추기]**
   → 말머리가 `데이터 모델링의 이해 | SQL 기본 및 활용 | 기타` 가 됩니다

### ③ 트래픽·하드 알림 — **아직 안 했습니다**

카페24 콘솔 → 서비스 사용현황 → 트래픽 알림 / 하드 알림 **설정하기 → 수신**

월 4,000MB 한도에 마이캐쉬 0원, 자동리셋 꺼짐입니다. **초과하면 사이트가 멈추는데 알림이 없으면 모릅니다.**

---

## 5단계 — 테스트

### A. 브랜드 · 헤더
☐ 전 화면 로고가 **XAM**(노랑)**pass**
☐ `AXEXAM` · `WOWPASS` 문자열이 어디에도 없다
☐ 푸터 문제집 목록이 DB 에서 온다 (SQLD 1개)

### B. 문제집 · 4탭
☐ `/exam/check.php` → 탭이 **홈 · 이론 · 문제집 · 과목게시판** 4개
☐ `/exam/check.html` 로 들어가면 `check.php` 로 넘어간다
☐ `/exam/check.php?pd=sqld&m=theory` → **이론 탭으로 열린다** (예전엔 홈으로 떨어졌다)
☐ 문제집 탭 → 50문제 · 과목 드롭다운 2개
☐ 보기 고르고 **채점하기** → 점수·해설. **정답 보기** 도 된다
☐ 새로고침 → "이전에 선택한 답안 N개를 불러왔습니다" + 선택 유지
☐ 과목 필터를 바꿔도 답안이 지워지지 않는다
☐ breadcrumb 이 `XAMpass › SQL 개발자(SQLD) 문제집 › 회차별 모의고사 › 1회`
☐ 도식(SVG)이 보인다 ← `pd/sqld/figs/` 경로 확인
☐ 이론 탭 → 요약노트 2개가 열린다
☐ **관리자에서 문제 한 건을 고치고 새로고침 → 반영된다** ← 예전엔 안 됐습니다

### C. 랜딩 · 상세
☐ `/exam/` 카드 **2개** — SQLD(열림) · 빅데이터분석기사 필기(**준비 중**)
☐ SQLD 카드 [자세히 보기] → `detail.html?pd=sqld`
☐ 상세 제목·회차·과목이 API 에서 온다 (SQLD 하드코딩 아님)
☐ `/exam/sqld.html` → `detail.html?pd=sqld` 로 넘어간다

### D. 수강 신청 — 문제집별
☐ `/exam/buy.php?pd=sqld` → 과정 **3개만** (6개가 뜨면 pd 필터 실패)
☐ 상단에 **[다른 문제집] 빅데이터분석기사 필기** 칩
☐ `?pd=bdae-w` → 빅분기 과정 3개
☐ 이름·연락처 넣고 제출 → 신청번호와 완료 화면
☐ **같은 문제집** 한 번 더 → "이 문제집에 이미 접수된 신청이 있습니다"
☐ ★ **다른 문제집** 신청 → **통과한다** ← 예전엔 원리적으로 막혀 있었습니다

### E. 관리자 · 포인트
☐ `/adm/exam_orders.php` → 신청 목록에 **문제집 열**이 있다
☐ **[승인]** → "월 지급 1건이 반영되었습니다"
☐ **승인을 한 번 더 누를 수 없다** (버튼이 사라진다)
☐ `/adm/exam_credit_grant.php` → 회원 조회 → 잔액·구독권·포인트 묶음·원장
☐ 강제 지급 1,000원 → **같은 회원에게 한 번 더 지급이 된다** ← 이게 막혀 있었습니다
☐ ★ SQLD 잔액만 늘고 **빅분기 잔액은 0 유지** ← 문제집별 분리 확인
☐ 상단 **포인트 정합성**이 `일치`
☐ 음수(-500) 조정 → 회수되고 원장에 `debit` 이 남는다

### F. 마이페이지 — 문제집 선택
☐ `/exam/mypage.php` → 상단에 **[내 문제집]** 칩 (승인된 것은 `· N개`)
☐ 칩을 누르면 `?pd=` 로 이동하고 **이전 문제집 데이터가 남지 않는다**
☐ '남은 질문' 이 문제집별로 다르다
☐ 응시 이력 · 오답노트 · 내 질문 3탭
☐ 내 질문이 **그 문제집 것만** 보인다

### G. 과목게시판
☐ `check.php` 과목게시판 탭 → 과목 칩 **전체 · 데이터 모델링의 이해 · SQL 기본 및 활용**
☐ [질문하기] → 게시판 글쓰기(새 창), **말머리가 선택된 과목으로 미리 채워져 있다**
☐ [전체 보기] → 게시판 목록

### H. API 직접 확인 (선택)

```
/exam/api/products.php                    자격증 2개 · href 가 detail.html
/exam/api/problems.php?pd=sqld            product.pd_name 이 있다
/exam/api/me.php                          books[] 에 내 문제집
/exam/api/board.php?pd=sqld               과목게시판 최근 글
```

---

## 문제가 생기면

| 증상 | 대처 |
|---|---|
| 사이트 전체 백지 | `/adm/` 은 살아 있다 → 환경설정에서 테마를 `basic` 으로. 또는 FTP 로 `/theme/axexam` 폴더명 변경 |
| 관리자 전체 백지 | `admin.menu600.exam.php` 를 지운다. 메뉴만 사라지고 나머지는 돈다 |
| 특정 페이지만 백지·500 | 그 PHP 파일만 지운다 |
| `check.php` 가 목록으로 튕긴다 | 문제가 0건인 품목이다. 재임포트를 확인한다 |
| 도식이 안 보인다 | `pd/sqld/figs/` 가 안 올라갔다 |
| 이론이 "찾을 수 없습니다" | `pd/sqld/theory_content.js` 가 안 올라갔다 |
| 과목게시판이 "게시판이 만들어지지 않았습니다" | 4단계 ② 를 아직 안 했다 |
| 포인트가 한 문제집만 들어온다 | 1단계 검증 (3) 을 확인한다 — `uq_month` 가 4컬럼인가 |

**PHP 는 크기로 비교하지 않습니다.** 응답만 나오고 소스가 안 나옵니다. 동작으로 확인합니다.
**정적 파일도 크기가 다르게 보입니다** — FileZilla 가 CRLF→LF 로 바꿔 올려 줄 수만큼 작아집니다. 정상입니다.

---

## 그다음

1. **빅분기 문제 임포트** — OCR/집필 후 `--pd bdae-w` 로 빌드 → `pd/bdae-w/` 업로드 → 임포트 → 게시판 1개 생성
2. **SQLD 7~21회분** — 같은 절차. `pd/sqld/` 만 다시 올립니다
3. **LLM 답변 초안(S8)** — DeepSeek 키 발급부터. `qa_draft` 컬럼·상태·검수 화면은 준비돼 있습니다
4. **포인트 차감 ON** — `ex_product.pd_config` 에 `{"charge":true}`. 코드 변경 0
5. **유튜브 미등록 업로드** — `data/youtube_map.json` 30개 비어 있습니다
6. **공지 게시판 상단 250px 빈 공간** — devtools 로 확인이 필요합니다
