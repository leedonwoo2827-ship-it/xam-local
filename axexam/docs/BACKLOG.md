# 백로그 — 지금 안 만들지만 결정은 해둔 것

계획서(`_context/`)에 없는, 작업 중에 나온 설계 판단을 모아둔다.

---

## ★ 채점 결과 → 분석 리포트 (다음 학습 추천)

**요구**: 채점이 점수만 보여주고 끝나는 게 아니라, **취약분야를 분석해 보관하고
"이 이론을 학습하라"까지 이어져야 한다.**

**디자인이 이미 있다** — `assets/present/` 의 출처인 UI 킷에
`result-report.html`(23KB)이 있고 구조가 정확히 이것이다:

```
예상 점수(보수적/일반/낙관적)  →  종합 결과  →  취약분야 분석(보완필요/일부보완/양호)
→  영역별 세부 문항 분석  →  AI 맞춤 분석 리포트  →  다음 학습 추천(→ 강의 보러가기)
```
원본: `D:\00work\260724-chodangi-mp4\_context\present\result-report.html`
(전산세무회계용이라 문구는 갈아끼워야 하지만 레이아웃·컴포넌트는 그대로 쓴다)

### 새 테이블이 필요 없다 — 데이터가 이미 다 있다

| 리포트 항목 | 출처 |
|---|---|
| 문항별 정오 | `ex_attempt_item (at_id, pr_id, chosen, is_ok)` |
| 응시 이력 보관 | `ex_attempt` — 점수·회차·시각·필터 |
| 과목별 정답률 | `ex_problem.sj_no` / `sj_name` 조인 |
| **취약 개념** | `ex_problem.tags_json` — 예: 슈퍼타입, 서브타입, 슈퍼/서브타입 변환 |
| 누적 오답 | `ex_wrong (try_cnt, wrong_cnt)` |
| **과목 → 이론 문서** | `theory.js` 의 `sub` 가 곧 `subject_no` **이미 매핑돼 있다** |
| 틀린 구간 → 해설영상 | `videos.js` 의 `part` = `ceil(문제번호 / 10)` |

→ **필요한 건 집계 쿼리와 화면뿐이다.** 스키마 변경 0.

### 만들 것

1. `api/report.php?at_id=` — 응시 1건의 분석 (과목별·태그별 정답률 + 추천)
2. `api/attempts.php` — 내 응시 이력 목록 (보관본 다시 보기)
3. `/exam/report.php` — `result-report.html` 레이아웃 이식
4. 추천 규칙 — 처음엔 단순 규칙으로 충분하다:
   - 과목 정답률 60% 미만 → 그 과목 이론 요약노트 링크
   - 오답이 2개 이상 몰린 태그 → 취약 개념으로 표시
   - 틀린 문제가 3개 이상인 파트 → 그 파트 해설영상 링크

### 주의

- **"AI 분석"이라고 쓰려면 실제로 LLM 을 태워야 한다.** 규칙 기반이면 그렇게 부르지 않는다.
  초기에는 규칙 기반으로 충분하고, 문구를 "학습 추천"으로 정직하게 쓴다.
- 태그가 문제당 2~3개라 표본이 작다. **응시 1회만으로 "취약"이라 단정하지 말고
  `ex_wrong` 누적치와 함께 본다.**
- 원본 리포트의 "예상 점수(보수적/일반/낙관적)"는 부분점수가 있는 시험용이다.
  SQLD 는 객관식이라 점수가 확정적이므로 **이 블록은 빼거나 다른 것으로 대체한다.**

---

## 엑셀/CSV 로 문제 업로드

**언제**: 새 자격증을 OCR 파이프라인(`02/`~`05/`) 없이 넣을 때,
또는 B2B 기관이 문제를 직접 넣겠다고 할 때 ([COST.md](../_context/COST.md) §10).

**그누보드에 이미 부품이 있다**

| 자산 | 위치 |
|---|---|
| PHPExcel 번들 | `lib/PHPExcel/`, `lib/PHPExcel.php` |
| 구형 Excel 리더/라이터 | `lib/Excel/` (`oleread.inc.php`, `php_writeexcel`, `reader.php`) |
| **엑셀 일괄 수정 레퍼런스** | `adm/shop_admin/itemexcelupdate.php` ← 영카트 상품 일괄 수정. 우리와 같은 구조 |
| 엑셀 내보내기 레퍼런스 | `adm/member_list_exel_export.php` |
| 파일 업로드 처리 | `adm/sms_admin/num_book_file_upload.php` |

**구조상 이미 준비돼 있다**: `exam_import.php` 는 JSON 을 배열로 만들 뿐이고,
검증·upsert·리포트는 전부 `exam_lib/problem.php` 의 `ex_import_problems($doc)` 안에 있다.
**파서만 갈아끼우면 된다.**

### ⚠ 조심할 것 셋

**① `pr_key` 안정성이 전부다 — 이게 유일한 진짜 위험이다**

파이프라인은 `m01-1#7` 을 기계가 만든다. 엑셀에서 사람이 손으로 넣으면
**오타 하나에 같은 문제가 새 행으로 들어가고 `pr_id` 가 갈린다.**
`ex_attempt_item` / `ex_wrong` 이 `pr_id` 를 참조하므로 회원 오답노트와
정답률 집계가 조용히 끊긴다. **되돌릴 수 없다.**

→ **"엑셀 업로드"가 아니라 "엑셀 왕복(export → edit → import)"으로 설계한다.**
   - 내보내기에 `pr_key` 를 **첫 열**로 박는다 (읽기 전용으로 안내)
   - 기존 문제는 그 `pr_key` 로 매칭한다
   - 신규만 `pr_key` 빈칸 → 서버가 규칙대로 생성 (`{pd_id}{rd:02d}-{part}#{no}` 같은 결정적 규칙)
   - 임포트 전에 **드라이런**(무엇이 신규/갱신/건너뜀인지만 보여주고 쓰지 않음)을 넣는다

**② PHPExcel 은 폐기된 라이브러리다.** 후속은 PhpSpreadsheet.
그누보드가 아직 품고 있지만 **PHP 8.4 에서 실제로 도는지 서버에서 확인해야 한다.**

**③ 진실 원천이 셋이 된다.** 지금은 `02/` → DB, 웹편집 → `edited_by` 보호.
엑셀이 붙으면 경로가 하나 더 늘어난다. **`edited_by` 보호 규칙을 엑셀 임포트에도 똑같이 적용**하고,
엑셀로 들어온 문제는 `02/` 에 원본이 없다는 것을 어딘가에 표시해야 한다
(`src_id` 를 비우거나 `src_from='xlsx'` 같은 표식).

### 더 싼 대안 — CSV

`fgetcsv()` 가 PHP 내장이라 라이브러리가 0이다. **90% 의 가치를 10% 의 비용으로.**
한글은 내보낼 때 UTF-8 BOM(`\xEF\xBB\xBF`)을 붙이면 엑셀이 제대로 연다.
읽을 때는 CP949 로 저장된 파일에 대비해 `mb_detect_encoding` → `mb_convert_encoding`.

단점: 지문·해설에 줄바꿈과 쉼표가 많아 셀 안에서 깨져 보인다. 편집 경험은 xlsx 가 낫다.

→ **먼저 CSV 왕복으로 만들고, 편집 불편이 실제로 문제가 되면 xlsx 로 올린다.**

---

## SFTP 전환

포트 22 는 열려 있다(실측). 막힌 건 계정 권한이므로
카페24 콘솔 `보안관리 → FTP/Shell 접속설정` 에서 Shell 을 켜면 된다.

지금은 평문 FTP + 국내 IP 제한으로 운영 중. PHP 편집 루프를 본격적으로 돌기 전에 전환한다.

---

## `02/` 역반영 절차

`edited_by` 가 채워진 문제(웹에서 고친 것)를 `D:\00work\ocr-output-260723\02\` 로
되돌리는 절차가 아직 없다. **하지 않으면 언젠가 "원본 복원"이 낡은 내용을 되살린다.**

최소안: `adm/exam_problem_list.php` 에 `edited_by <> ''` 필터를 두고,
목록을 마크다운으로 내보내 `02/` 에 수동 반영. 주기는 월 1회.

---

## 영카트 `shop/` 처리

쓰지 않는데 설치돼 있다. 삭제하면 `adm/admin.menu400/500.shop_*.php` 가 남아 메뉴가 깨지고,
그누보드 업데이트마다 되살아난다. **그냥 두고 패치만 유지**하기로 했다(공격면은 늘지만 삭제 리스크가 더 크다).

나중에 정리한다면: `shop/` 에 `.htaccess deny` + 관리자 메뉴에서 숨김.
