# 업로드 목록 (2026-07-31) — 성적표 델타

로컬 `D:\00work\260729-new\` · 서버 웹루트 `/www`

**어제 13:13 이후 작업분이 서버에 없습니다.** 실측으로 확인했습니다 —
`/exam/report.php` 가 404 이고 `check.php` 렌더 결과에 `성적표` 문자열이 없습니다.

**DB 변경이 없습니다.** 새 컬럼도 새 테이블도 없고 전부 집계 쿼리입니다.
그래서 순서 제약이 없습니다 — 10개를 한 번에 올리면 됩니다.

> `06\` 빌드 산출물(`index.html`·`detail.html`·`pd/sqld/`)과 `api/videos.php`·`api/board.php` 는
> **이미 최신입니다.** 로컬과 서버 md5 가 같습니다(개행 정규화 후). 다시 올리지 않습니다.

---

## 1단계 — 파일 12개 (10분)

### 신규 — 없는 파일이라 그냥 올립니다

| 로컬 | 서버 | 크기 |
|---|---|---|
| `web\exam\report.php` | `/www/exam/report.php` | 3.2KB |
| `web\exam\api\report.php` | `/www/exam/api/report.php` | 16KB |
| `web\exam\assets\report.css` | `/www/exam/assets/report.css` | 10KB |
| `web\exam\assets\report.js` | `/www/exam/assets/report.js` | 19KB |
| **`web\exam\api\attempts.php`** | `/www/exam/api/attempts.php` | 2.2KB |
| **`web\exam\api\wrong.php`** | `/www/exam/api/wrong.php` | 3.4KB |

> ⚠ **아래 두 개는 07-29 에 만들어졌는데 한 번도 안 올라갔습니다** (실측: 둘 다 404).
> `mypage.js` 가 이 둘을 호출하므로 **마이페이지의 응시 이력·오답노트 탭이 이틀째 죽어 있었습니다.**
> 07-29 체크리스트의 "신규" 목록에 있었는데 07-30 문서로 넘어가면서 빠진 것으로 보입니다.
> 성적표도 응시 이력에서 들어가는 경로가 있어서 **이 둘이 없으면 성적표를 올려도 반쪽입니다.**

### 덮어쓰기

| 로컬 | 서버 | 왜 |
|---|---|---|
| `web\exam\check.php` | `/www/exam/check.php` | **`<div id="rpLink">` 자리.** 없으면 [성적표 보기] 가 그려질 곳이 없습니다 |
| `web\exam\assets\check.js` | `/www/exam/assets/check.js` | 채점 후 `rpLink` 를 채웁니다 |
| `web\exam\assets\check.css` | `/www/exam/assets/check.css` | `.side-actions` |
| `web\exam\assets\mypage.js` | `/www/exam/assets/mypage.js` | 응시 이력 → 성적표 링크 |
| `web\exam\assets\mypage.css` | `/www/exam/assets/mypage.css` | **`.rp` 색 토큰이 여기 있습니다.** 없으면 성적표 색이 깨집니다 |
| `web\exam\assets\gnuboard-skin.css` | `/www/exam/assets/gnuboard-skin.css` | **푸터 4단 그리드 수정** (성적표와 무관하지만 같이 밀렸습니다) |

### 삭제 — 없음

### 선택 — 과목게시판 예시 글

지금 `sqld_sj` 게시판이 **글 0건**입니다(`api/board.php?pd=sqld` → `items:[]`).
방문자에게 빈 게시판이 보입니다. 채우려면:

| 로컬 | 서버 |
|---|---|
| `_probe\exam_demo_qna.php` | `/www/exam_demo_qna.php` ← **웹루트 최상위** |

최고관리자로 접속 → [예시 글 넣기] → **끝나면 파일을 지웁니다.**

---

### 올린 뒤 한 명령으로 확인합니다

```
python scripts/deploy_check.py
```

공개 URL 만으로 **업로드 누락·삭제 누락·API 응답**을 전부 판정합니다.
지금 돌리면 실패 14건이 나오고, 위 12개를 올리면 0건이 되어야 합니다.
개행(CRLF→LF)을 정규화해서 md5 를 비교하므로 **크기 차이는 무시해도 됩니다.**

---

## 2단계 — 확인 (5분)

### A. 성적표 — 핵심 경로

☐ `/exam/check.php?pd=sqld` → 문제 풀고 **채점하기**
☐ 우측에 **[성적표 보기]** 버튼이 나타나는가 ← `check.php` + `check.js` 둘 다 올라가야 나옵니다
☐ 누르면 `/exam/report.php?pd=sqld&at=N` 으로 가는가
☐ 성적표에 **과목별 취약도 · 취약 개념 · 문항별 · 반복 오답 · 다음에 볼 것**이 순서대로 나오는가
☐ 색이 깨지지 않는가 (깨지면 `mypage.css` 가 안 올라간 것입니다)
☐ 비로그인으로 `/exam/report.php` → 로그인 페이지로 보내는가

> 비회원은 채점 기록이 남지 않습니다(`grade.php` 가 `mb_id` 있을 때만 INSERT).
> 로그인이 필요한 건 정책이 아니라 구조입니다.

### B. 마이페이지 — 이틀째 죽어 있던 두 탭

☐ `/exam/mypage.php` → **응시 이력** 탭에 채점 기록이 나오는가 ← `attempts.php`
☐ **오답노트** 탭에 틀린 문제가 나오는가 (내가 고른 보기 빨강 / 정답 초록) ← `wrong.php`
☐ 응시 이력의 각 행에서 성적표로 넘어가는가
☐ 성적표에서 마이페이지로 돌아오는 길이 있는가

> A 에서 **로그인 상태로 채점을 먼저** 해야 이 둘에 데이터가 생깁니다. 순서대로 하는 게 좋습니다.

### C. 푸터

☐ `/exam/` 맨 아래 **4단이 붙어 있지 않고** 그리드로 떨어지는가
☐ 링크끼리 붙어 있지 않은가

### D. 회차 잠금 — 지금은 확인할 수 없습니다

`api/report.php` 는 `ex_round.rd_free = 0` 인 회차의 성적표를 수강생에게만 엽니다.
**그런데 지금 SQLD 6회차가 전부 `rd_free = 1`** 입니다(실측). 전면 공개 상태입니다.

잠금 동작을 보려면 회차를 내려야 합니다:

```sql
UPDATE ex_round SET rd_free = 0 WHERE pd_id = 'sqld' AND rd_no > 1;
```

**정책 결정 사항입니다.** 지금은 6회차 전부 무료 개방이 의도된 상태일 수 있으니
결정 전에는 실행하지 않습니다. 재임포트가 `rd_free` 를 건드리지 않으므로
언제 바꿔도 유지됩니다.

---

## 3단계 — 아직 안 한 것 (어제·그제 문서에서 넘어온 것)

### ① 카페24 트래픽·하드 알림 — **3일째입니다**

콘솔 → 서비스 사용현황 → **트래픽 알림** · **하드 알림** → 설정하기 → 수신.

월 4,000MB 한도 · 마이캐쉬 0원 · 자동리셋 꺼짐입니다.
**초과하면 사이트가 멈추는데 알림이 없으면 모릅니다.**

### ② 관리자 화면 4개 — 밖에서 확인이 안 됩니다

로그인 후 주소로 직접 확인합니다. 백지·500 이면 **그 파일만 지웁니다** — 관리자는 살아 있습니다.

```
/adm/exam_orders.php        수강 신청 승인
/adm/exam_credit_grant.php  포인트 지급
/adm/exam_qna_list.php      질문 검수
/adm/exam_board_sync.php    과목게시판 말머리
```

☐ `/adm/` 좌측 메뉴에 **문제집** 대역이 보이는가 (`admin.menu600.exam.php` 가 올라갔는가)
☐ `/adm/exam_credit_grant.php` 상단 **포인트 정합성 `일치`**

### ③ `uq_month` 가 4컬럼인가 — 어제 마이그레이션 검증 (3)

```sql
SHOW INDEX FROM ex_credit_lot WHERE Key_name = 'uq_month';
```

`(mb_id, pd_id, lot_src, lot_period)` 4행이어야 합니다.
**3행이면** 두 문제집을 수강하는 회원의 같은 달 두 번째 지급이 DB 차원에서 거부됩니다.
에러가 안 나서 나중에 원인을 찾기가 매우 어렵습니다.

---

## 확인된 서버 상태 (2026-07-31 오전 실측)

밖에서 볼 수 있는 것은 전부 확인했습니다. `python scripts/deploy_check.py` 로 언제든 다시 봅니다.

| ✅ 되어 있음 | 근거 |
|---|---|
| 문제 재임포트 | 회차 라벨 `모의고사 01회`~`06회` · 300문제 · 2과목 |
| 과목게시판 `sqld_sj` + 말머리 3개 | `api/board.php?pd=sqld` |
| `06/` 빌드 · `pd/sqld/` 업로드 | 로컬과 md5 동일 |
| 옛 납작 사본 4개 삭제 | `problems.js`·`videos.js`·`theory.js`·`theory_content.js` 전부 404 |
| `exam_migrate.php` 삭제 | 404 (보안상 중요) |
| `.htaccess` 차단 | `exam/sql/*.sql` · `problems.json` 403 |
| 해설영상 API | `api/videos.php?pd=sqld` → `{"ok":1,...,"items":{}}` |

---

## 문제가 생기면

| 증상 | 대처 |
|---|---|
| [성적표 보기] 가 안 나온다 | `check.php`(자리) 와 `check.js`(채우는 쪽) **둘 다** 필요합니다 |
| 성적표 색이 깨진다 | `mypage.css` 가 안 올라갔습니다 — `.rp` 토큰이 거기 있습니다 |
| 성적표가 500 | `api/report.php` 만 지우면 나머지는 돕니다 |
| `at_id` 가 없다고 나온다 | 로그인 상태로 채점을 먼저 해야 기록이 남습니다 |
| 특정 페이지만 백지·500 | 그 PHP 파일만 지웁니다 |
| 사이트 전체 백지 | `/adm/` 은 살아 있습니다 → 테마를 `basic` 으로. 또는 FTP 로 `/theme/axexam` 폴더명 변경 |

**PHP 는 크기로 비교하지 않습니다.** 응답만 나오고 소스가 안 나옵니다. 동작으로 확인합니다.
**정적 파일도 크기가 다르게 보입니다** — FileZilla 가 CRLF→LF 로 바꿔 올려 줄 수만큼 작아집니다. 정상입니다.