# 남은 작업 — 체크리스트

마지막 갱신: 2026-08-10. 끝낸 것은 지우지 말고 `[x]` 로 둔다 — 왜 그렇게 했는지가
다음 사람에게 필요하다.

**돌려서 확인하는 법**: `set PYTHONPATH=.` → `venv\Scripts\python tools\smoke_authoring.py`
(81건 · 약 3초 · 모델 안 부름 · 무료)

---

## Phase 5 — 앱 안에서 집필 (거의 끝)

- [x] `services/authoring/provider.py` — CLI 탐지(VSCode 확장 폴백 포함) · 구독 OAuth · 계량 회수
- [x] `services/authoring/errors.py` — 인증/한도/그밖 3분류
- [x] `services/authoring/schema.py` — `_rounds` 문항 스키마 (실측 키 `question_no`)
- [x] `services/authoring/spec.py` — 집필 규칙 · 과목별 난이도 계획
- [x] `services/authoring/draft.py` — 파트 집필 → 검증 → 스테이징
- [x] `services/authoring/merge.py` — 스테이징 → `_rounds` UPSERT (백업 · 부분반입 금지)
- [x] `services/authoring/derive.py` — `build.py`·`validate.py` 서브프로세스 + 로컬 교정 감시
- [x] `vendor/exambook/` — `build.py`·`validate.py` 수정 0줄로 들이기 + README
- [x] `routes/authoring_routes.py` + `app.py` 등록
- [x] `static/js/authoring.js` + 사이드바 항목 + 어두운 레일 CSS
- [x] `docs/새-책-폴더-만들기.md` — `00/`·`01/` 만 복사한다
- [x] `tools/smoke_authoring.py` — 81건 통과
- [x] **분량 재검증 — 3회 교정으로 수렴했다** (각 20문항 실측):
      | 차수 | 처방 | 화면 | 낭독 | 판정 |
      |---|---|---|---|---|
      | 1차 | "화면의 3~4배"(비율만) | 919자 | 1552자 | ✗ 편당 47분 |
      | 2차 | `maxLength` 상한 | 147자 | 272자 | ✗ 편당 8.3분 (과교정) |
      | 3차 | 상·하한 + "구간 위쪽을 겨냥" | **166자** | **336자** | ✓ m01 기준(166/356) |
      기준 m01 = 화면 166 · 낭독 356. 화면은 **정확히 일치**, 낭독은 94%.
      난이도(상8·중11·하1)와 정답분포(5/5/5/5)는 3회 모두 요청대로 나왔다.
      **교훈: 비율로 지시하면 안 된다.** 화면과 낭독이 같이 부풀면 비율은 그대로다.
      그리고 모델은 제시 구간의 **아래쪽에 붙는다** — 하한도 스키마로 강제해야 한다.
- [ ] 비용 — 과목당 $1.4(캐시 있음) ~ $3.6(캐시 없음). **1시간 프롬프트 캐시가 이 차이다.**
      회차 4과목을 **연달아** 돌리면 첫 과목만 비싸다. 화면이 그렇게 돌리도록 만들어 뒀다
- [ ] **회차 통째로 1회** — 4과목 집필 → 반입 → 파생 → `#/questions` 에 뜨는지.
      지금은 파트 단위까지만 실증했다
- [ ] `?ver=` 캐시 무력화 — `app.css`·`shell.js` 에 없어서 지금은 Ctrl+F5 가 필요하다
- [ ] 커밋·푸시

---

## Phase 1·2 — 슬라이드 (기하 완료 · 분할 남음)

원본은 `showcase-agent/render/slides.py` 다(`_deck.css` 가 아니다 — 2026-08-10 지시).

- [x] `services/deck/` — `theme.py`(기하 단일 소유자) · `slidecss.py` · `render.py` · `preview.py`
- [x] **★ 그림을 화면 최상단 꽉** — `order:-1`. **실측 61/61 (100%)** 로 카드 내용
      영역 최상단에 정확히, 가로 꽉. `h2::after` 밑줄은 아예 넣지 않았다(17px 절약)
- [x] **★ 그림 높이 상한** `--fig-max`. 비율은 **예산에서 거꾸로 계산**해 0.42(273px)
- [x] `/slide/` 미리보기 + `?fit=1` (`/print/` 패턴 재사용, `def` 로)
- [x] `tokens.css` 분리 → `theme.py` 가 읽어 **deck.html 에 인라인**
      (★ axexam 복사본이 두 벌이라 `260729-new` 와 프로젝트 양쪽에 적용했다)
- [x] 덱 클래스명을 사이트 것으로 통일 (`.qcard`·`.q`·`.opts`·`.opt`·`.expl`)
- [x] 해설 면에서 보기 4개 반복 제거 — 228px(예산 35%)를 먹었다. 정답 배지에 **보기
      글자까지** 넣어 대체(영상은 되돌려 볼 수 없다)

**실측 (Chromium 1920×1080 · 7번들 140장):** 넘침 11장 → **1장(1%)** · 보기 전부보임
45/70 → **62/70** · 그림 상단정렬 **61/61**

- [ ] **분할(paginate)** — 남은 8장은 발문이 2~3줄인 문항이다. 상수로는 더 못 줄인다.
      순서: `ch2` → 안 들어가면 쪼갠다 → `dense` → 그래도 넘치면 그 번들만 실패
- [ ] `.trunc-note` 폐기 (지금은 경고만 찍고 잘린 슬라이드가 유튜브까지 간다)
- [ ] `deck_fit.json` 사이드카 + `precheck` 게이트 3개
      (부재 / `over>0` / `review_slides != scene_total`)
- [ ] `?fit=1` 이 실제로 Chromium 으로 재서 `data-over` 를 심게 (지금은 띠만 그린다)
- [ ] 씬 생성(`build_scenes`) — **분할 결과에서 파생**되므로 분할 뒤에 붙인다.
      하드 게이트(`.slide` 수 == `capture:true` 씬 수)를 만족해야 베이크 경로에 붙일 수 있다
- [ ] 드리프트 탐지 — `tokens.css`·`slidecss.py` mtime 을 `bundles.py:101` 입력에
- [ ] `store.py:341-363` 의 "도구 #2 로 다시 만들고" → 로컬 재생성 버튼 안내
- [ ] 무거운 작업 락을 `services/jobs/locks.py` 한 곳으로

> **★ axexam 복사본이 두 벌이다** — 앱은 `XAM_AXEXAM=D:\00work\260729-new`(git 클론,
> remote=axexam.git)를 읽는데, 업로드는 프로젝트 안 `axexam/` 에서 해 왔다. 네 파일
> 해시가 전부 다르다. **어느 쪽을 정본으로 할지 정해야 한다.** 지금은 양쪽에 같은
> 변경을 넣어 어느 쪽을 쓰든 동작하게 두었다.
- [ ] `store.py:341-363` 의 "도구 #2 로 다시 만들고" → 로컬 재생성 버튼 안내
- [ ] 무거운 작업 락을 `services/jobs/locks.py` 한 곳으로 (덱 빌드도 Chromium 을 띄운다)

> **★ #2 를 서브프로세스로 부르지 않는다.** `bundle.py:961-962` 가 `05/` 를 `04/` 로
> 덮어 **8번들의 로컬 교정이 되돌아간다.** 호출이 아니라 포팅이다.

---

## Phase 3 — 모바일

- [ ] 표·그림 넘침 — `minmax(0,1fr)` (`check.css:28,88,95`) + `.tscroll` 래퍼를
      `tableHtml()`(SQLD) **과** `mdTable()`(빅분기) **양쪽에**. 한쪽만 고치면 한 문제집만 고쳐진다
- [ ] `img.fig{max-width:min(520px,100%)}` · `.expl{white-space:pre-wrap}` 제거
- [ ] 하단 시트 — `.side` **자체**를 `position:fixed` 로 (복제하면 id 중복 →
      `updateGauge()`·`showScore()`·`#rpLink` 가 어느 쪽을 잡을지 갈린다)
- [ ] `viewport-fit=cover` 는 Phase 0 에서 끝났다 → `env(safe-area-inset-bottom)` 사용 가능
- [ ] 640px 밀도 블록 (`table.qtable th,td` 를 `7px 14px → 6px 9px`)
- [ ] `features.php` 캡처 순서 — Phase 0 에서 `order:-1` 로 이미 고쳤다. 폰에서 확인만

---

## Phase 4 — 나레이션

- [ ] 발음사전에 `ㄱ:기역 ㄴ:니은 ㄷ:디귿 ㄹ:리을 ㅁ:미음`
      (`pronunciation_map.yaml` — **데이터만, 코드 0줄**)
      → 자막은 `ㄱ` 원문 유지, 음성만 `기역`. 사전이 합성 직전에만 걸리기 때문이다
- [ ] 기존 9회차의 손으로 쓴 `그/드/르` 되돌리기 (~72건 · 회차당 8~9건).
      먼저 **후보만 출력해** 눈으로 확인한 뒤 바꾼다("그리고" 를 잡으면 안 된다)
- [ ] `과목`·`회` 를 `_SINO_SPACED_UNITS` 에 (`1과목`·`01회` 가 날것으로 TTS 로 간다)
- [ ] 도입부 문장 — "N번부터 M번까지" · "8편 중 3편". 번호는 `problem` 블록 `number` 에서
- [ ] `narration` 이 `_strip_brand()` 를 안 타서 `자사` 가 소리로 나간다 → 통과시킨다
- [ ] 자막 원문 — **`s.speech` 가 빈 씬(cover·section)에만** `srt_text` 를 채운다.
      problem/answer 에 채우면 말하지 않는 자막이 흐른다

---

## 보류 (사용자가 다시 꺼낼 것)

- [ ] `m01-3#21` 복수정답 (`값 −18` → `−10`) — 응시 0건인 지금이 안전한 창
- [ ] SQLD 프롬프트 — 지금 것은 80문항/회차용. SQLD 는 50문항이라 그대로 쓰면
      `pr_key` 가 밀려 응시 100건이 끊긴다
- [ ] 인쇄본 검수 피드백 (열 폭 · 여백)
- [ ] 모바일 게시판 스킨 (`mobile/skin/board/basic` 복사)
- [ ] 12개월 총량형 질문권 1,200개 (`pl_grant` 열 · `ex_settle()` 월 루프 우회)
- [ ] `bdae-w` 정리 (그 품목의 `ex_plan` 3행을 먼저 지운 뒤 품목 삭제)
- [ ] `adm-qna-draft.png` → `/www/exam/shots/`
- [ ] 이론편 — `03/summary_*.html` 은 **이미 HTML** 이라 lesson JSON 이 아니다.
      새로 필요한 것은 "HTML → Block 목록" 변환. **문제편이 초록이 된 뒤에** 착수
