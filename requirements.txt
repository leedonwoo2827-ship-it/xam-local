# XAM LOCAL — 로컬 실행 의존성. LLM 없음 · 인증 없음 · DB 없음.
#
# 선택 의존성을 0 으로 유지하는 것이 setup.bat 의 실패 지점을 없애는 실질적 이득이다.
# sha256=hashlib · ZIP=zipfile · SVG 검사=xml.etree · 원자적 쓰기=tempfile+os.replace
# 잡=threading+subprocess · 한글 파일명 헤더=urllib.parse.quote — 전부 표준 라이브러리.

fastapi>=0.115     # starlette>=0.40 필요 — FileResponse 의 Range 지원(mp4 드래그 탐색)
uvicorn
python-dotenv
PyYAML             # 02/*.md 는 읽기 전용(쓰기는 손으로 조립).
                   # 01/*.md 는 **쓰기도** PyYAML 이다 — 도구 #1 이 safe_dump 로 찍었고
                   # 손으로 조립하면 source_pages 블록 목록의 들여쓰기가 어긋난다.

# ── 도구 #1(OCR 검수) 내장분 ────────────────────────────────────────────────
pypdfium2          # 00/*.pdf → data/raw_pages/*.png (pip 전용·크로스플랫폼)
Pillow             # 스캔 이미지에서 그림 영역(bbox) 크롭 · 슬라이드 렌더

# ── 도구 #3(영상 렌더 엔진) 내장분 — vendor/chodangi ────────────────────────
# 예전에는 chodangi 저장소의 별도 .venv 가 이걸 들고 있었다. 엔진이 안으로 들어와서
# venv 하나로 합쳤다. 렌더를 안 쓰면 설치만 되고 로드되지 않는다(늦은 import).
numpy>=1.26        # 무음 씬 wav 생성
soundfile>=0.12.1  # wav 길이 실측 (자막 시간축의 기준)
onnxruntime>=1.17  # Supertonic3 TTS. GPU 를 쓰려면 onnxruntime-gpu 로 교체
pydantic>=2.6      # voicewright 대본 스키마
pysrt>=1.1.2       # 자막
lxml>=5.2.0        # mp4maker (MLT XML)
playwright>=1.44   # deck.html → 슬라이드 캡처 (Chromium)
#
# ★ 이 둘은 pip 로 안 된다 — 바이너리라서 저장소에도 못 넣는다. setup.bat 이 확인한다.
#     ffmpeg                                  PATH 에 있어야 한다
#     python -m playwright install chromium   브라우저 1회 설치
#   그리고 TTS 모델(vendor/chodangi/assets/, 395MB)은 .gitignore 대상이다.
