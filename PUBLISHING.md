# 발행·수정 규칙 (거버넌스)

2026-08-03 확정. 이 문서가 절차의 정본이다 — 화면의 체크리스트(`#/publish` ⑥)도 같은 내용을
폴더에서 계산해 보여준다. 숫자는 여기 적지 않는다(회차가 늘면 낡는다).

---

## 1. 원천은 하나다 (single source of truth)

| 대상 | 원천 | 거쳐 가는 곳 | 서버가 읽는 것 |
|---|---|---|---|
| 문항 | `_rounds/*.json` | `02/*.md` · `05/lesson` → `06/problems.json` | **DB** (관리자 임포트) |
| 영상 링크 | `data/youtube_map.<pd>.json` | 빌드 | `pd/<pd>/videos.private.json` (**런타임 파일**) |
| 요약노트 | `03/summary_*.md` | `.html` → `theory_content.js` | 정적 |
| 그림 | `02/assets` · `01/images` | `06/pd/<pd>/figs/` | 정적 |

**서버의 파일을 직접 고치지 않는다.** 다음 빌드가 덮어써서 원천이 둘로 갈린다.
고칠 곳은 늘 위 '원천' 열이다.

**문항은 `#/questions` 에서만 고친다.** `_rounds`·`02/`·`05/lesson` 세 파일을 앱이 함께
갱신하고 **바이트 단위로 왕복 검증**한다. 손으로 하나만 고치면 나머지와 갈리고 저장이 409 로
막힌다.

---

## 2. 1차 배포 = 레벨 제한 (강사·SME·운영자만)

영상을 구글 드라이브에 올려 **`min_level: 5`** 로 낸다. 일반 회원·비로그인은 보지 못한다.

```json
{ "_provider": "drive",
  "videos": { "m01-1": { "id": "<공유 URL 또는 ID>", "label": "1회 1~10번",
                         "sec": 0, "min_level": 5 } } }
```

**왜 레벨이 필수인가** — 드라이브·`link`·`file` 은 **링크 자체가 접근 권한**이다.
`min_level ≤ 1` 이면 빌더가 그 링크를 `videos.js`(정적 파일)에 굽고, 그러면 로그인하지 않은
사람도 파일을 내려받아 링크를 얻는다. JS 에서 버튼을 숨겨도 파일 안에 남는다.

| `min_level` | 링크가 가는 곳 | 누가 받나 |
|---|---|---|
| ≤ 1 | `videos.js` (정적) | **누구나** |
| ≥ 2 | `videos.private.json` → `api/videos.php` | `mb_level` 이 그 값 이상인 로그인 사용자 |

`api/videos.php` 는 요청마다 그 JSON 을 다시 읽는다(캐시·DB 없음). 그래서 레벨을 바꾸거나
링크를 바꿀 때 **그 파일 하나만** 올리면 즉시 반영된다.

공개로 전환할 때는 유튜브로 옮기고 `min_level` 을 내린다. 항목별 `provider` 가 파일 전역
`_provider` 를 이기므로 **일부만 먼저 옮겨도 된다.**

---

## 3. 변경 비용 — 무엇을 고치면 어디까지 다시 하나

### ① 영상 링크만 (교체·삭제·추가) — **가장 싸다**

```
매핑 수정 → 빌드 → pd/<pd>/videos.private.json 1파일 FTP
```
`problems.json` 재임포트 없음. 관리자 화면 없음. `min_level ≤ 1` 이면 대신 `videos.js`.

★ 드라이브·유튜브에서 **파일을 지우고 다시 올리면 ID 가 바뀐다.** 내리지 말고 비공개로 둔다.

### ② 문항 내용만 (영상에 안 나오는 부분) — **중간**

```
#/questions 수정 → 빌드 → problems.js 등 FTP → 관리자 화면에서 problems.json 임포트
```
임포트 리포트의 '갱신' 수가 고친 문항 수와 같아야 한다. `pr_key` 로 UPDATE 하므로
**회차·번호를 바꾸면 새 행이 생긴다**(옛 행은 남는다).

### ③ 문항이 영상에도 나온다 — **가장 비싸다. 그래서 앱이 대신 추적한다**

문항을 고치면 `05/lesson` 이 갱신되고, 그러면 그 번들의 mp4 가 낡는다.

**앱이 자동으로 잡는다** — `services/render/bundles.py` 가
`mp4 mtime < max(deck, script, lesson)` 이면 그 번들을 **`stale`(재렌더)** 로 표시한다.
`lesson.json` 이 비교에 들어 있는 것이 핵심이다.

```
#/questions 수정
  → #/video 의 '재렌더 N' 카운터에 뜬다  → [미완성만 렌더] 로 그 번들만 다시 만든다
  → 그 mp4 만 다시 업로드 → 매핑의 id 교체 → videos.private.json 1파일 FTP
  → (문항도 바뀌었으므로) problems.json 임포트
```
재렌더 단위는 **번들(10문항)** 이다. 1문항만 고쳐도 그 1편은 다시 만들어야 한다.
72편 전체가 아니다.

사전점검(`#/publish` ①)의 `mp4 가 deck·lesson 보다 새롭다` 가 같은 것을 경고로 잡는다.
문항 데이터만 먼저 넘길 거면 그 경고는 무시해도 된다.

---

## 4. 하지 말 것

- **서버 파일 직접 수정** — 다음 빌드가 덮어쓴다. 원천을 고친다.
- **`--book` / `--pd` 생략** — 둘 다 기본값이 SQLD 라서 **라이브 SQLD 문제은행을 덮어쓴다.**
  `pr_key` 가 겹치고 `pr_id` 는 보존되어 회원 오답노트 밑에 엉뚱한 문제가 앉는다.
- **`02/`·`_rounds`·`05/lesson` 을 손으로 고치기** — 세 파일이 갈리면 저장이 막힌다.
- **모르는 `provider` 값** — 화면이 유튜브로 간주해 빈 재생창이 뜬다. 빌더가 죽인다.
  네이버 마이박스는 embed 가 없으므로 `link`(새 창)를 쓴다.
- **영상 파일을 지우고 다시 올리기** — ID 가 바뀐다. 비공개로 두고 교체한다.
- **`sec` 에 영상 길이를 넣기** — `sec` 은 **시작 초**다. `check.js` 가 `&start=` 로 쓴다.
  길이를 넣으면 모든 영상이 끝에서 시작한다.

---

## 5. 미결 (추후 과제)

- **문항별 시간 이동** — 지금 웹은 번들 하나에 시작점 하나(`sec`)만 쓴다.
  문항별로 뛰려면 웹(`check.js` `embedUrl()`)·빌더 스키마를 늘려야 한다.
  **데이터는 이미 있다** — `#/publish` ② 표가 번들마다 문항별 시작 초를 보여준다
  (`review.json` 의 `startSec`, 영상 시간축 기준으로 2026-08-03 에 맞춤).
- **관리자 화면에서 영상 설정** — `videos.private.json` 이 런타임 파일이라 가능해 보이지만
  그누보드 쪽 화면을 만드는 일이고, 원천이 둘로 갈릴 위험이 있다. 지금은 하지 않는다.
- **영상 길이** — 현재 편당 4~6분이다. 15분 목표는 `#2` 프롬프트(`260803`)의
  "문항당 낭독 90초 · explanation 을 문단으로 길게" 가 반영되면 달성된다.
- **사이트 구조** — 9회차·720문항이 들어간 뒤 구조를 바꿔야 하는지는 아직 보지 않았다.
