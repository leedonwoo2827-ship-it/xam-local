<?php
/**
 * /www/index.php — 루트를 문제집 포털로 보낸다.
 *
 * ⚠⚠ 이 파일은 **그누보드 코어 파일을 교체한 것**이다.
 *     그누보드를 업데이트하면 원본으로 되돌아가서 루트에 그누보드 메인이 다시 뜬다.
 *     → docs/DEPLOY.md 의 업데이트 절차에 "이 파일을 다시 덮는다" 가 적혀 있다.
 *
 *     .htaccess 리다이렉트(web/.htaccess)를 쓸 수 있으면 그쪽이 낫다 —
 *     배포본에 없는 파일이라 업데이트에 살아남는다. 이건 그게 여의치 않을 때의 수단이다.
 *     둘 다 걸어두면 어느 한쪽이 날아가도 노출되지 않는다.
 *
 * 원본 index.php 가 하던 일: theme/basic/index.php 를 include 해서
 * 최신글 위젯이 깔린 그누보드 메인을 그린다. 우리는 그 화면을 쓰지 않는다.
 * (원본은 gnuboard5-5.6.34 릴리스에 그대로 있고 git 에도 남아 있다.)
 */

// 절대 URL 대신 상대 경로를 쓴다 — 커스텀 도메인을 붙여도 그대로 동작한다.
header('Location: ./exam/', true, 301);
exit;
