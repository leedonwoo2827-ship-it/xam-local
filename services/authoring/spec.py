# -*- coding: utf-8 -*-
"""집필 규칙 — 시스템 프롬프트와 파트 프롬프트를 조립한다.

★ 출처: `_docs/exam-all-빅분기-프롬프트-260803.md`(사내 문서, 저장소에 없다). 이 파일은
  그 문서의 지시를 **파트 단위 호출에 맞게 접은** 것이다.

★ 난도 루브릭·오답쌍·함정·과목별 계획·약어 발음은 **여기 없다.** `exams/<pd>.json` 에서
  읽는다(`system()` 이 조립한다) — 품목마다 다르기 때문이다. 빅분기 문장을 여기 두었더니
  SQLD 집필이 시험에 없는 개념으로 갔다(2026-08-19).

★ 이 파일이 프롬프트의 유일한 소유자다. 화면·라우트에 프롬프트 문자열을 두지 않는다.
  두 곳에 두면 한쪽만 고쳐지고, 그 차이는 문항을 읽어 봐야 안다.

한 호출 = 한 파트(보통 10문항)인 이유는 비용이다. 호출당 바닥값이 약 $0.25 다
(하네스 시스템 프롬프트가 캐시생성 42,852토큰). 문항당 부르면 10배가 된다.
자세한 실측은 `provider.py` 머리말에 있다.
"""
from __future__ import annotations

from typing import List

from .schema import subjects

# ── 시스템 프롬프트 ─────────────────────────────────────────────────────────
# ★ 파트마다 **바이트 단위로 같아야 한다.** 여기가 프롬프트 캐시의 접두다.
#   회차별 값(회차코드·번호)을 섞으면 파트마다 캐시를 새로 만들어 비용이 4배가 된다
#   (실측: 캐시 읽기가 걸리면 $0.257 → $0.066).
SYSTEM_TEMPLATE = """\
{머리}

■ ★★ 불변 원칙 — **시험에 나오는 대로 맞춘다**

이 문서의 모든 규약보다 위에 있다. 아래 지침과 실제 시험이 어긋나면 **시험이 맞다.**

  · 문항의 꼴 — 도식을 주고 묻는 문항이면 도식을 준다. 조합형("모두 고른 것은")을
    쓰는 시험이면 쓴다. 시험이 안 쓰는 꼴을 새로 만들지 않는다.
  · 자료의 꼴 — 시험이 표로 주면 표로, 그림으로 주면 그림으로 준다.
    말로 풀어 쓰는 것은 시험과 다른 물건을 만드는 것이다.
  · 묻는 방식 — 부정형·계산형·순서형의 비중을 시험에 맞춘다.
  · 난도 — 타사 판이 아니라 **실제 시험** 수준에 맞춘다.

수험생이 이 문제집으로 연습한 뒤 시험장에서 **처음 보는 꼴을 만나지 않아야 한다.**
그것이 이 문제집의 유일한 존재 이유다. 분량·개수·모양의 편의보다 이것이 앞선다.

■ 최우선 — 난도를 실전 수준으로
타사 모의고사(`01/`)의 분포는 실제 시험보다 쉽다. 표면만 고치면 쉬운 문제집이 복제된다.
타사 판은 "무엇이 나오는가" 의 근거로만 쓰고, 난도는 개념을 실전 수준까지 새로 설계한다.
derived_from 은 추적용이지 난도 기준이 아니다.

{시험정보}

■ ㄱ/ㄴ/ㄷ/ㄹ 조합형을 쓸 때
화면 표기는 **`ㄱ` `ㄴ` `ㄷ` `ㄹ` 원문 그대로** 쓴다. 시험지에 그렇게 나온다.
`그` `느` `드` `르` 같은 발음체로 쓰지 말 것 — 자막에 그것이 그대로 나가고
회차마다 표기가 흔들린다. 낭독을 자모 이름으로 읽는 일은 렌더 단계의
발음사전이 합성 직전에 처리한다. 집필은 원문만 쓴다.

■ ★ 분량 — 이것을 지키는 것이 내용보다 중요하다
영상 1편(10문항)이 약 15분이어야 한다. 길이를 넘기면 편당 40분이 넘고 슬라이드 수와
음성 합성 시간이 세 배가 된다.

  explanation        : **160~260자.** 상한 320자.
  explanation_speech : **350~450자.** 상한 520자. ★ 350자를 넘겨라.

기준은 이미 만들어 검증된 회차의 실측 중앙값이다 — 화면 166자 · 낭독 356자.
그 회차가 편당 15분을 냈다. **그 길이를 맞추는 것이 목표다.**

★ 짧은 쪽으로도 틀린다. 실측에서 이 지시로 낭독 272자가 나와 편당 8.3분이 됐다
  (목표 15분). 구간을 주면 아래쪽에 붙는 경향이 있으니 **구간 위쪽을 겨냥해서 쓴다.**
  반대로 상한을 넘기면 편당 40분이 넘고 슬라이드 수와 음성 합성 시간이 세 배가 된다.
  넘치지도 모자라지도 않게 — 낭독은 400자 근처가 좋다.

■ explanation (화면·자막) — 150~250자
아래를 **빈 줄로 문단 분리**해 쓴다. 전부 넣으려 하지 말고 이 문항에서 중요한 것만
고른다.
  1) 정답 근거 + 핵심 공식/수치      ← 반드시
  2) 핵심 오답 1~2개가 어디서 어긋나는지  ← 4개를 다 해부하지 않는다
  3) 실전 포인트 한 줄 (필요할 때만)

■ explanation_speech (낭독) — 300~450자
"정답은 N번입니다." 로 시작한다.

★ **화면(explanation)에 있는 것을 말한다.** 화면에 없는 내용을 덧붙이는 것을
  최소로 한다. 낭독이 길어지는 원인이 정확히 이것이다 — 오답 해부의 상세, 시험장
  풀이 순서, 암기 근거, 비슷한 문항과의 비교를 낭독에만 얹으면 분량이 세 배가 되고
  **화면에 없는 말을 듣는 시간**이 그만큼 늘어난다. 시청자는 화면을 보고 있다.
  덧붙일 가치가 있는 것은 화면(explanation)에 쓰고, 낭독은 그것을 말로 풀어라.

  (원 집필 프롬프트는 *"화면에 없는 내용을 읽어도 된다"* 고 했다. 그것이 편당
   40분을 만든 원인이라 뒤집었다 — 2026-08-10 지시.)

낭독은 발문·보기를 다시 읽지 않는다. 표와 그림도 읽지 않는다 —
화면에 이미 있고, 낭독 시간은 해설에 쓴다.

순수 텍스트만 쓴다. 마크다운(`**` 강조 · 백틱 · `![]()`)을 넣지 않는다.
★ **발음으로 바꿔 쓰지 않는다.** 약어는 약어 그대로, 기호는 기호 그대로 쓴다(`1.2%`).
  이 값은 **자막이 된다**(`bake._narration`). 「에스큐엘」 이라고 쓰면 화면에도 그것이
  나가고, 회차마다 표기가 흔들린다. 소리로 바꾸는 일은 렌더 단계가 한다 —
  발음사전(`pronunciation_map.yaml`)과 숫자 규칙이 합성 직전에만 적용된다.
  ㄱ/ㄴ/ㄷ/ㄹ 을 원문으로 쓰는 것과 같은 이유다(위 항목 참조).
괄호 (…) 부가설명은 낭독에서 빼고 화면(explanation)에만 남긴다.

■ ★ 그림(SVG) — **지문의 그림은 답을 가리고, 해설의 그림은 온전하게**
`assets` 에 인라인 SVG 를 넣고, 그 그림을 참조하는 줄을 본문 안에 둔다.

★ 그림을 지문에 두는 것은 **막지 않는다.** 막는 것은 **답이 보이는 것**이다.
  기출이 도식을 주고 묻는 문항을 그렇게 낸다 — 그림을 빼면 문항이 성립하지 않는다.
  그래서 자리로 가르지 않고, **같은 그림을 두 판으로** 그린다.

  · 지문 판 : 답이 되는 자리를 **가린다.** 그 자리에 `(  )` · `?` · 빈 칸을 두고
              나머지는 그대로 그린다. 보는 사람이 구조는 알고 답은 모른다.
  · 해설 판 : **온전하게** 그린다. 가렸던 자리를 채우고, 필요하면 강조한다.
              정답을 밝힌 뒤이므로 감출 것이 없다.

  이름은 지문 판이 `mNN-QQ-키워드`, 해설 판이 `mNN-QQ-키워드-답` 이다.

  assets      : [{"name": "mNN-QQ-키워드",     "svg": "…빈 칸이 있는 판…"},
                 {"name": "mNN-QQ-키워드-답",  "svg": "…채운 판…"}]
  passage     : … 조건 …
                ![도식 제목](assets/mNN-QQ-키워드.svg)
  explanation : … 문단 …
                ![도식 제목](assets/mNN-QQ-키워드-답.svg)

★ 세 갈래로 나뉜다. 어느 쪽인지 먼저 정한다.

  ① **답이 그림 안에 글자로 있다** → 두 판을 그린다(위 규약).
{가림예시}
     분류 체계 · 단계 이름 · 용어 대응표가 다 여기다.

  ② **그림에 답이 애초에 없다** → 지문에 그대로 둔다. 해설 판은 만들지 않는다.
     ERD(관계도)를 보고 조인 결과를 묻는 문항, 테이블 구조를 보고 SQL 결과를
     묻는 문항이 그렇다. 그림은 답이 아니라 **주어진 자료**다.
     말로 풀어 쓰지 않는다 — 관계선·카디널리티는 글로 옮기면 뭉갠다.

  ③ **문제와 무관하게 이해를 돕는 그림** → 해설에만 둔다.
     개념을 그림으로 보여야 이해가 되는 설명용 도식이다.

★ 스스로 검산할 것: **지문에 넣은 그림만 보고 답을 고를 수 있으면 안 된다.**
  고를 수 있으면 아직 가리지 않은 것이다 — 그 자리를 비우고 해설 판으로 옮긴다.
  가릴 방법이 마땅치 않으면 그 그림은 해설에만 둔다(노출이 더 큰 손해다).

★ 그림은 **그 문항에 필요하면 넣는다.** 개수를 맞추려고 넣거나 빼지 않는다.
  · ② 조건 그림은 문항의 일부다. 도식을 주고 묻는 문항이면 반드시 그린다.
  · ①③ 은 그림이 이해를 실제로 바꿀 때만 넣는다 — 글로 충분한 것을 그리면
    화면만 길어지고 읽는 사람이 얻는 것이 없다. 넣을지 애매하면 넣지 않는다.
  · ① 의 두 판은 지문 판을 복사해 **한 곳만** 바꾸는 것이 가장 짧고 안전하다.
  · 세로로 길게 그리지 않는다. 슬라이드 한 장에 그림과 해설이 함께 들어가야 하므로,
    가로로 퍼뜨리고 여백을 줄인다(`viewBox` 의 높이를 폭보다 크게 잡지 않는다).
{그림주제}

★ SVG 규칙 — 캡처가 `file://` 로 열리고 1920×1080 에 얹히므로:
  · `viewBox` 를 반드시 넣고 `width`/`height` 는 넣지 않는다(가로 꽉 차게 늘어난다)
  · 글자는 `font-size="13"` 이상. 그보다 작으면 1080p 영상에서 못 읽는다
  · 외부 폰트·이미지·스크립트를 참조하지 않는다. 자체완결이어야 한다
  · 4,000자를 넘기지 않는다. 넘으면 응답 토큰을 먹어 뒤 문항이 잘린다
  · `name` 은 `mNN-QQ-키워드` 형태로 문항 id 를 앞에 둔다(다른 회차 그림과 안 겹치게)

■ passage
지문이 필요한 문항에만 쓴다. 필요 없으면 **빈 문자열**로 둔다.
도식이 필요한 문항은 **말로 풀지 말고 그린다**(ERD·테이블 구조·계층 트리).
그림에 답이 적혀 있으면 그 자리를 `(  )` 로 비우고, 온전한 판은 해설에 둔다.
— 위 「그림(SVG)」 항목의 세 갈래를 따른다. 조건도 답도 아닌 장식은 넣지 않는다.

■ tags
2~4개. 개념 이름으로 쓴다(유형 이름이 아니다).
"""


# ── 파트 프롬프트 ──────────────────────────────────────────────────────────
# ── 집필 기준 ───────────────────────────────────────────────────────────────
# 두 가지다. 화면에서 고르고 **기본은 `exam`(시험기준)** 이다(2026-08-10 지시).
#
#   exam   : **기본.** 기출을 보지 않고 실제 시험 기준으로 새로 설계한다.
#            `derived_from` 은 빈 문자열.
#   derive : 기출의 **주제·개념만 유지**하고 문장·수치·맥락을 전면 교체한다.
#            ★ 목적이 "파생" 이 아니라 **저작권 회피**다. 같은 주제를 다루되 기출의
#              표현을 재사용하지 않는 것이 요구다.
#            `derived_from` 에 기출 id 를 적어 추적을 남긴다.
#
# ★ 처음에 `derive` 를 "기출에서 파생한다" 로만 썼다 — 틀렸다. 그렇게 쓰면 모델이
#   기출 문장을 손질하는 쪽으로 가고, 그것이 정확히 피하려는 것이다. 그리고 원 집필
#   프롬프트가 적어 둔 함정에도 걸린다: *"기출의 자체 분포는 실제 시험보다 쉽다.
#   표면만 재작성하면 쉬운 문제집이 복제된다."*
#   두 모드 모두 **난이도는 실전 기준**이고, 기출은 난이도의 기준이 아니다.
# 화면 라벨 — 사용자가 정한 문구다(2026-08-10). 순서가 곧 콤보 순서이고 **앞이 기본값**.
MODES = ("exam", "derive")
MODE_LABEL = {"exam": "시험기준으로", "derive": "연습문제화"}


def part_prompt(*, round_code: str, numbers: List[int], subject_nos: List[int],
                difficulty_ask: str, derive_hint: str = "",
                mode: str = "derive",
                round_index: int = 0, round_total: int = 0) -> str:
    """이 파트에서 무엇을 쓸지. **회차마다 달라지는 것만** 여기 담는다.

    ★ 시스템 프롬프트(SYSTEM)와 섞지 않는다 — 그쪽은 캐시 접두라 고정이어야 한다.

    `derive_hint` 는 기출 파일명 목록이다. 파일 내용을 넣지 않고 **이름만** 준다:
    모델이 Read 로 필요한 것만 읽는 편이, 240문항을 프롬프트에 붓는 것보다 싸다
    (실측: 문항 1개에 `Read:01/01-01.md` 1회로 끝났다).
    `mode="exam"` 이면 이 목록을 주지 않는다.
    """
    # ★ 과목 범위를 **시험정보에서** 만든다. `(n-1)*20+1~n*20` 은 빅분기에서만
    #   맞는 식이었고, SQLD(10 / 40)에서는 1과목을 「1~20번」이라고 모델에게 말했다.
    from services.authoring import parts as _P

    _spec = _P.active()
    _names = subjects()
    _bounds, _acc = {}, 0
    for _s in (_spec or {}).get("subjects") or []:
        _c = int(_s.get("count") or 0)
        if _c > 0:
            _bounds[int(_s.get("no") or 0)] = (_acc + 1, _acc + _c)
            _acc += _c
    subj_lines = "\n".join(
        f"    {n}과목 {_names.get(n, '')} — 문항번호 "
        f"{_bounds.get(n, (0, 0))[0]}~{_bounds.get(n, (0, 0))[1]}"
        for n in sorted(set(subject_nos)))
    rng = f"{numbers[0]}~{numbers[-1]}" if len(numbers) > 1 else str(numbers[0])

    if mode == "exam" or not derive_hint:
        # ★ 용어를 **타사/자사**로 쓴다(2026-08-19 지시).
        #   `01/` 은 **다른 출판사의 모의고사집**을 판독한 것이다. 시행처 기출이 아니다.
        #   그래서 표현을 재사용하면 그 출판사의 저작물을 침해한다 — 주제·꼴만 본다.
        #   ("자사" 는 음성·화면에 넣지 않는다. 프롬프트 안에서만 쓰는 말이다.)
        #
        # ★ 전에는 「기출을 읽을 필요가 없다」 였다. 그것이 틀렸다 —
        #   「주제는 OCR 에서라도 잘 찾아오면 됩니다. 그래서 OCR 을 하는 거고요」,
        #   「타사 모의고사가 집필하기 편하게 되어 있다」 는 것이 판독하는 이유다.
        basis = ("\n"
                 "■ 집필 기준 — **난도를 실전 수준으로.**\n"
                 "  난도는 타사 모의고사를 따르지 않는다. 타사 판은 실제 시험보다 쉽다 —\n"
                 "  출제 범위의 개념을 **실전 수준으로 새로 설계한다.**\n"
                 "  ★ 다만 **주제와 꼴은 타사 판에서 확인한다.** 작업 폴더의 `01/` 에 판독해 둔\n"
                 "    타사 모의고사가 있고, 집필하기 좋게 정리돼 있다. Grep·Read 로 이 파트\n"
                 "    문항번호대를 훑어 다음 셋을 본다 — 「시험에 나오는 대로」 의 근거다:\n"
                 "      · 어떤 주제가 실제로 나오는가 (필수주제의 확인·보완)\n"
                 "      · 도식·표·SQL 을 주고 묻는 문항인가 (그러면 우리도 그렇게 낸다)\n"
                 "      · 묻는 말투 — 부정형·계산형·조합형의 비중\n"
                 )
        basis += COPYRIGHT_RULES + (
                 "  derived_from 은 **빈 문자열**로 둔다.\n"
                 )
    else:
        # ★ 목적이 저작권 회피다. "파생" 이라고만 쓰면 모델이 타사 문장을 손질하는
        #   쪽으로 가는데, 그것이 정확히 피하려는 것이다. 무엇을 바꿔야 하는지 나열한다.
        basis = (f"\n■ 집필 기준 — **타사 판의 주제만 유지한다. 목적은 저작권 회피다.**\n"
                 f"  작업 폴더의 `01/` 은 **다른 출판사의 모의고사집**을 판독한 것이다.\n"
                 f"  아래 중에서 골라 **Read 로 읽고**, 그 문항이 **무엇을 묻는지(주제·개념)만**\n"
                 f"  가져온다.\n"
                 f"  {derive_hint}\n"
                 )
        basis += COPYRIGHT_RULES + (
                 "  ★ 난도는 **타사 판을 따르지 않는다.** 타사 판은 실제 시험보다 쉽다 —\n"
                 "    주제만 가져오고 난도는 위의 실전 기준으로 올린다.\n"
                 f"  derived_from 에는 주제를 가져온 타사 문항의 id(파일명에서 `.md` 를 뗀 것)를 쓴다.\n"
                 )

    # ★ 회차 차별화. 회차마다 **따로** 호출되므로 서로를 볼 수 없다 — 아무 말도 안 하면
    #   같은 하위개념이 회차마다 반복된다. 원 프롬프트도 같은 것을 요구했다:
    #   *"9회차를 서로 다른 하위주제로 차별화하고."*
    diff = ""
    if round_total > 1 and round_index:
        diff = (f"\n■ 회차 차별화 — 이것은 {round_total}회차 중 **{round_index}회차**다.\n"
                f"  다른 회차와 **같은 하위개념을 반복하지 않는다.** 같은 대주제라도\n"
                f"  다른 각도(정의 ↔ 계산 ↔ 사례 판단)와 다른 세부 항목으로 낸다.\n"
                + ("  뒤 회차이므로 '상' 난이도를 구간 상단에 맞춘다.\n"
                   if round_index > round_total / 2 else ""))

    return f"""\
회차 {round_code} 의 문항 {rng} ({len(numbers)}문항)을 집필한다.

■ 과목 배정 — 문항번호로 정해진다. 바꾸지 않는다.
{subj_lines}

■ 이 파트의 난이도
  {difficulty_ask}
{basis}{diff}
■ 지킬 것
  · question_no 는 {rng} 안의 값만 쓰고, {len(numbers)}개를 **빠짐없이** 쓴다.
  · 정답 위치(answer_index)를 한쪽에 몰지 않는다.
  · 계산형은 계산을 끝까지 해서 정답을 확정한 뒤 보기를 만든다.
    보기 4개 중 오답 3개는 **틀리는 방식이 서로 달라야** 한다
    (예: 복제 계수를 빼먹은 값 / 압축을 평균으로 계산한 값 / 단위를 틀린 값).
"""



def _exam() -> dict:
    """지금 작업 폴더의 시험정보. 없으면 빈 dict — 그때는 되돌림 문구를 쓴다."""
    from services.authoring import parts

    return parts.active() or {}


def _head(d: dict) -> str:
    """프롬프트 첫 줄 — 무슨 시험의 집필자인가."""
    label = str(d.get("label") or "").strip() or "자격시험"
    n = int(d.get("choices") or 4)
    return f"당신은 {label} 문제집의 집필자다. {n}지선다만 쓴다."


def _exam_block(d: dict) -> str:
    """★ 루브릭·오답쌍·함정·분포를 **시험정보에서** 만든다.

    전에는 이 블록이 코드에 박혀 있었다 — 「정밀도↔재현율」 「KDD·CRISP-DM·SEMMA」
    처럼 빅분기 용어가 그대로였다. SQLD 를 그 프롬프트로 집필하면 시험에 없는 개념으로
    문항이 나온다. 「시험에 나오는 대로 맞춘다」 가 불변 원칙이므로, 이 값들은 코드가
    아니라 `exams/<pd>.json` 에 있어야 한다(2026-08-19).
    """
    NLC = chr(10)
    out: list = []
    dif = d.get("difficulty") or {}
    rub = dif.get("rubric") or {}
    if rub:
        out.append("난이도 루브릭")
        for lv in (dif.get("levels") or ["하", "중", "상"]):
            if rub.get(lv):
                out.append(f"  {lv} : {rub[lv]}")
    tgt = dif.get("round_target") or {}
    if tgt:
        seg = []
        for lv, v in tgt.items():
            seg.append(f"{lv} {v[0]}~{v[1]}" if isinstance(v, (list, tuple)) and len(v) == 2
                       else f"{lv} {v}")
        out.append("회차 목표 분포: " + " · ".join(seg))
    pairs = d.get("distractor_pairs") or []
    if pairs:
        out.append("")
        out.append("오답(distractor)은 수험생이 실제로 혼동하는 개념 쌍에서 가져온다. "
                   "무의미한 보기 금지.")
        # 한 줄에 셋씩 — 한 줄로 길게 늘어놓으면 모델이 앞쪽만 본다
        for i in range(0, len(pairs), 3):
            out.append("  " + " / ".join(str(x) for x in pairs[i:i + 3]))
    traps = d.get("traps") or []
    if traps:
        out.append("전형적 함정: " + " · ".join(str(x) for x in traps))
    neg = d.get("negative_stem_per_round") or []
    if isinstance(neg, (list, tuple)) and len(neg) == 2:
        out.append(f"부정형 발문(…않은 것은?)은 회차당 {neg[0]}~{neg[1]}문항.")
    out.append("계산형은 정답을 반드시 직접 검산해 확정한다.")
    return NLC.join(out)


def system() -> str:
    """집필 시스템 프롬프트. **품목마다 다르다** — 시험정보에서 조립한다.

    ★ 캐시 접두는 여전히 고정이다. 여기 들어가는 값은 시험 단위(품목)이고
      회차·파트마다 바뀌지 않는다 — 한 품목을 연달아 집필하는 동안 같은 문자열이다.
    """
    d = _exam()
    return (SYSTEM_TEMPLATE.replace("{머리}", _head(d))
            .replace("{시험정보}", _exam_block(d))
            .replace("{그림주제}", _fig_topics(d))
            .replace("{가림예시}", _masked_example(d)))


def _fig_topics(d: dict) -> str:
    """그림이 잘 맞는 주제 — 시험정보의 `figure_topics` 에서.

    ★ 없으면 **예시를 주지 않는다.** 빅분기 주제(혼동행렬·ROC)를 되돌림값으로
      두면 SQLD 집필이 그것을 보고 시험에 없는 그림을 그린다 — 예시 없는 편이 낫다.
      대신 과목의 `must_cover` 를 근거로 삼으라고 말한다.
    """
    ft = d.get("figure_topics") or {}
    rows = []
    for key, mark in (("masked", "①"), ("condition", "②"), ("explain", "③")):
        v = [str(x) for x in (ft.get(key) or [])]
        if v:
            rows.append(f"                {mark} " + " · ".join(v))
    if not rows:
        return ("  어떤 주제에 그림이 맞는지는 **그 과목의 `반드시 다룰 것`** 에서 고른다"
                + chr(10) + "  (아래 파트 지시에 나온다). 시험에 없는 도식을 새로 만들지 않는다.")
    return "  잘 맞는 주제 —" + chr(10) + chr(10).join(rows).lstrip()


def _masked_example(d: dict) -> str:
    """①(답을 가리는) 갈래의 예시 한 줄 — 시험정보의 `figure_masked_example` 에서."""
    ex = str((d.get("figure_topics") or {}).get("masked_example") or "").strip()
    if not ex:
        return ("     그림에 선택지의 답이 글자로 적혀 있으면, 묻는 자리를 `(  )` 로 비운 판을"
                + chr(10) + "     지문에 두고 채운 판을 해설에 둔다.")
    return "     " + ex



# ★ 저작권 조항 — **두 모드가 같은 문장을 쓴다.**
#
#   `01/` 은 시행처 기출이 아니라 **다른 출판사의 모의고사집**을 판독한 것이다
#   (2026-08-19 확인). 주제·개념은 누구의 것도 아니지만 **표현은 그 출판사의 것**이다.
#   그래서 「읽어오되 저작권에 위배되지 않게 수정한다」 가 규칙이고, 시험기준 모드도
#   타사 판을 읽으므로 이 규칙이 똑같이 걸린다. 한 곳에 두면 두 갈래가 갈리지 않는다.
COPYRIGHT_RULES = (
    "  " + chr(9733) + " 읽되 **다음을 전부 바꾼다.** 하나라도 남으면 저작권 회피가"
    " 성립하지 않는다:\n"
    "    · 문장 — 발문·보기·해설의 표현을 재사용하지 않는다. 어순만 바꾸는 것도 안 된다.\n"
    "    · 수치 — 표·계산의 숫자를 전부 새로 잡고 정답을 다시 검산한다.\n"
    "    · 맥락 — 사례·회사·상황 설정을 다른 것으로 바꾼다.\n"
    "    · 보기 구성 — 오답을 그대로 옮기지 않고 혼동 지점을 새로 고른다.\n"
    "    · 표·도식 — 열 이름과 값을 새로 잡는다. 구조가 같아도 내용이 같으면 안 된다.\n"
)


def difficulty_plan(n_parts: int) -> List[str]:
    """파트별 난이도·필수주제 요청문. **시험정보의 `per_subject_plan` 에서 만든다.**

    ★ 전에는 네 문장이 코드에 박혀 있었다 — 「KDD·CRISP-DM·SEMMA」 처럼 빅분기
      주제가 그대로였다. SQLD 를 그대로 돌리면 시험에 없는 개념을 필수로 요구한다.

    ★ 파트가 과목보다 많을 수 있다(큰 과목을 쪼갠 경우). 그때는 **그 파트가 속한
      과목의** 문장을 준다 — 과목 순서로 돌려 쓰면 2과목 파트가 1과목 지시를 받는다.
      쪼갠 과목이면 「그중 k/n」 이라고 알려 준다(그대로 주면 문항수 요구가 배가 된다).
    """
    from services.authoring import parts as _P

    spec_ = _P.active() or {}
    plans = {int(x.get("no") or 0): x for x in (spec_.get("per_subject_plan") or [])}
    ps = _P.parts_of(spec_)
    out: List[str] = []
    for p_ in ps[:max(0, int(n_parts))]:
        e = plans.get(int(p_.get("subject_no") or 0)) or {}
        ask = str(e.get("ask") or "").strip()
        cover = [str(x) for x in (e.get("must_cover") or [])]
        k_, tot = p_.get("of_subject") or (1, 1)
        seg = []
        if ask:
            seg.append(ask if tot == 1
                       else f"이 과목 전체 목표는 「{ask}」 이고 이 파트는 그중 "
                            f"{k_}/{tot} 이다 — 그 비율만큼 낸다.")
        if cover:
            seg.append("반드시 다룰 것: " + " · ".join(cover)
                       + ("" if tot == 1 else " (과목 안에서 나눠 다룬다)"))
        out.append(" ".join(seg) or "이 파트의 난이도는 회차 목표 분포에 맞춘다.")
    while len(out) < max(0, int(n_parts)):
        out.append(out[-1] if out else "회차 목표 분포에 맞춘다.")
    return out
