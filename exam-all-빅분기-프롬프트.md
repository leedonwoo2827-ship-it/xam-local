# /exam-all 실행 프롬프트 — 빅데이터분석기사 필기

> 클로드 데스크탑에 **아래 코드블록 전체를 그대로 붙여넣기**.
> 플러그인(exambook-forge)은 이미 설치돼 있고, 코드는 수정하지 않는다.
> 사전 준비는 playwright 1회 설치뿐 (맨 아래 참고).

---

```
/exam-all rounds=3 book=D:\00work\ocr-output-260730 chunk=10 subject=빅데이터분석기사 theme=teal

■ 품목: 빅데이터분석기사 필기 (SQLD 아님). SQL 문항 없음, 통계·알고리즘·평가지표 중심.
  references/bdae-written-syllabus.md 를 개념 풀로 쓰고,
  references/round-data-schema.md 와 authoring-rules.md 의 난이도·분포·과목비율 기준은
  SQLD 50문항용이므로 아래로 전면 대체한다.

■ ★ 최우선 지시 — 난이도를 실전 수준으로 올릴 것
  기출 01/ 의 자체 분포는 하 28 / 중 46 / 상 6 으로 실제 시험보다 쉽다.
  표면만 재작성하면 쉬운 문제집이 그대로 복제된다.
  기출은 "출제 개념의 근거"로만 쓰고, 난이도는 syllabus 개념을 실전 수준까지
  끌어올려 새로 설계한다. derived_from 은 추적용이지 난이도 기준이 아니다.

  목표 분포 (합 80): 상 28~32 · 중 42~46 · 하 6~8
  → 기존 권장(상 12~16)의 약 2배. 라벨만 상으로 바꾸지 말고 아래 루브릭을 실제로 충족시킬 것.

■ 난이도 루브릭 (빅분기 필기용 — SQL 실행추적 기준을 대체)
  하 : 단순 정의·분류 암기 1단계   ← 회차당 8문항 이하
  중 : 개념 비교·2단계 추론, 표/그래프 해석, 절차 순서 판단
  상 : 아래 중 하나 이상을 반드시 포함
       (a) 수치 계산 후 판단 — 혼동행렬→정밀도/재현율/F1, 지지도·신뢰도·향상도,
           IQR 이상값 경계, 엔트로피·지니·정보이득, 오즈비, R²·수정R², RMSE/MAE 비교
       (b) 두 개 이상 개념 결합 — 불균형 데이터에서 평가지표 선택, 다중공선성 진단 후
           규제 기법 선택, 차원축소 후 군집 평가 등
       (c) 표·시나리오 지문 해석 — round JSON 의 table 필드를 적극 사용
       (d) ㄱ/ㄴ/ㄷ/ㄹ 조합형 "모두 고른 것은" (회차당 6~10문항)

■ 난도를 만드는 오답(distractor) 설계 — 이게 핵심입니다
  오답은 실제 수험생이 혼동하는 개념 쌍에서 가져올 것. 무의미한 보기 금지.
    정밀도 ↔ 재현율 / 민감도 ↔ 특이도 / 회귀지표 ↔ 분류지표
    정규화 ↔ 표준화 / PCA ↔ 요인분석 / 배깅 ↔ 부스팅 / L1 Lasso ↔ L2 Ridge
    지지도 ↔ 신뢰도 ↔ 향상도 / 1종오류 ↔ 2종오류 / 과대적합 ↔ 과소적합
    KDD ↔ CRISP-DM ↔ SEMMA 단계 순서 / 가명처리 ↔ 익명처리 ↔ 총계처리
  전형적 함정 적극 출제: 불균형 데이터에서 정확도의 함정, AUC 해석,
    재현율↔정밀도 트레이드오프, 상관관계≠인과관계, 표본오차 vs 비표본오차.
  부정형("옳지 않은 것") 발문 회차당 15~20문항, 부정어는 굵게 표시.
  계산형은 회차당 20문항 이상, 정답은 반드시 직접 검산해 확정.

■ 회차 구성
  회차당 80문항 = 4과목 × 20, 전 문항 4지선다
    subject_no 1  빅데이터 분석 기획   question_no  1~20
    subject_no 2  빅데이터 탐색        question_no 21~40
    subject_no 3  빅데이터 모델링      question_no 41~60
    subject_no 4  빅데이터 결과 해석   question_no 61~80
  subject 문자열은 위 네 개를 한 글자도 바꾸지 말 것
    (요약노트 <h1>N과목 · 이름</h1> 과 일치해야 성적표 링크가 붙음)
  정답 분포: ①②③④ 각 15~25
  출제 순서: derived_from 순서를 섞어 배치
  3회차는 서로 다른 하위주제를 강조해 차별화하고, m03 로 갈수록 상 비중을 상단(32)에 맞출 것

■ ★ 낭독본은 화면 해설의 재낭독이 아니라 "강사의 말"로 쓸 것
  explanation(화면·자막) 과 explanation_speech(낭독) 는 독립 필드입니다.
  화면에 없는 내용을 읽어도 됩니다 — bundle.py 가 낭독본을 버리지 않고 분배합니다.

  explanation        : 짧게. 정답 근거 + 핵심 공식/수치. 슬라이드에 들어갈 분량만.
  explanation_speech : 길게. 화면 분량의 2~3배. "정답은 N번입니다." 로 시작하고 아래 포함.
    (a) 오답 4개를 하나씩 짚어 "왜 그럴듯한지" → "어디서 어긋나는지" 순으로 설명
        (화면엔 정답 근거만 쓰고, 오답 해부는 낭독으로만 가도 됩니다)
    (b) 실전 비법 — 이 유형이 시험에 나오는 방식, 헷갈리는 개념 쌍의 구분 요령,
        암기법(예: "재현율은 실제 양성 기준, 정밀도는 예측 양성 기준 — 분모가 다르다"),
        계산 문항이면 시험장에서 빨리 푸는 순서
    (c) 연결 개념 한 줄 — 같은 과목 내 함께 나오는 개념, 다음 문항과의 연결
  낭독본은 순수 텍스트만 (마크다운 **·백틱·![]() 금지), 발음 표기로
    (RMSE→"알엠에스이", R²→"결정계수", F1→"에프원 스코어", AUC→"에이유씨",
     K-fold→"케이 폴드", 1.2%→"일 점 이 퍼센트")
  괄호 (…) 부가설명은 낭독본에서 빼고 화면 자막에만 남길 것

  ※ 화면 해설(explanation)은 1페이지에 들어갈 분량으로 유지하세요.
    여러 페이지로 쪼개지면 낭독본이 "표시 분량 비율"로 나뉘어,
    화면에 없는 비법 문장이 엉뚱한 페이지에 배치됩니다.
    비법이 길어 화면 전환이 필요하면 explanation 끝에 "▶ 실전 포인트" 한 줄을 남겨
    분할 지점을 만들어 주세요.

■ 산출물 — 04·05 는 선택이 아니라 필수입니다
  02/  문제 MD (mNN-01 ~ mNN-80) + _index.json + difficulty_stats.json + assets/*.svg
  03/  요약(이론) 원고 HTML+MD — 기존은 03/_backup_<날짜>/ 로 백업 후,
       기출 01/ + 자사 m01~m03 전 회차 병합 재생성.
       난도가 올라간 만큼 계산 공식(정밀도·재현율·F1·향상도·정보이득·R²)은
       유도 과정까지 요약노트에 실을 것.
  04/  lesson_mNN.json (영상 대본)
  05/  mNN-1 … mNN-8 번들 — 회차당 8편(80÷10), 3회차 = 총 24편
       각 편에 source/deck.html + _deck.css/_deck.js + 부분 lesson,
       images/slide_*.png, script/mNN-K_script.json, review.json 스켈레톤
  ※ generate-mockexam SKILL 의 "웹-only 품목(bdae-w)은 04/05 생략 가능" 은 적용하지 말 것.
     6~8단계를 모두 실행합니다.

■ 슬라이드 잘림 방지 — bundle.py 전에 1회 반드시
  pip install playwright && python -m playwright install chromium
  난도 상승으로 해설이 길어져 슬라이드 넘침이 늘어납니다.
  "[warn] 슬라이드 N 이 …px 넘칩니다" 가 뜨면 deck.html 을 손대지 말고
  해당 문항의 lesson 해설을 줄인 뒤 bundle.py 를 다시 돌릴 것.
  (낭독본 explanation_speech 는 줄이지 마세요 — 슬라이드 높이와 무관합니다.)

■ 집필 품질
  SVG 를 아낌없이 쓸 것 — 혼동행렬, ROC 곡선, 편향-분산 트레이드오프, 덴드로그램,
    CRISP-DM 단계, 박스플롯/IQR 경계, K-fold 분할, 마진/커널, 지니 vs 엔트로피 곡선
  tags 2~4개, derived_from 은 01/ 의 실제 기출 문항 id (01-01 ~ 01-80)

■ 진행
  validate.py 오류 0 확인 → build.py → bundle.py 순서
  ※ validate.py 의 "정답 분포 편중 (각 8~17 권장)" 경고는 50문항용 하드코딩이라
    80문항이면 항상 뜹니다. 오류가 아니므로 무시하고 진행하세요.
  회차 단위로 끊어서 보고 (m01 완료 → m02 → m03)
  각 회차 보고 시 난이도 분포와 함께 "상 문항 중 계산형 몇 개 / 조합형 몇 개" 를 밝힐 것
  마지막에 회차별 난이도·정답 분포, 번들 편수(24), 산출 경로를 표로 정리
```

---

## 사전 준비 (최초 1회)

```
pip install playwright && python -m playwright install chromium
```

플러그인 수정이 아니라 **파이썬 패키지 설치**다. 없으면 `bundle.py`가 슬라이드 페이지
분할을 건너뛰고 경고만 남겨서 **슬라이드가 잘린 채로 나온다.**

## 왜 이 인자들인가 — 기본값이 전부 SQLD다

| 항목 | 플러그인 기본(SQLD) | 이 프롬프트(빅분기 필기) |
|---|---|---|
| `subject` / `theme` | SQLD / sqld | 빅데이터분석기사 / **teal** |
| 회차당 문항 | 50 (2과목 10:40) | **80 (4과목 × 20)** |
| 난이도 분포 | 상 12~16 / 중 24~28 / 하 8~10 | **상 28~32 / 중 42~46 / 하 6~8** |
| 정답 ①②③④ 각 | 8~17 | **15~25** |
| `chunk=10` 결과 | 회차당 5편 | **회차당 8편** → 3회차 **24편** |

`theme=teal` 은 `bundle.py` 의 실재하는 팔레트다(`#0D9488`). 미지정 시 sqld 파랑으로 떨어진다.
24편은 XAM LOCAL README 의 "240문항 · 영상 24편" 과 일치한다.

## 플러그인은 건드리지 않는다

- 난이도 분포는 `validate.py` 의 검증 대상이 **아니다**(출력만 한다). 마음껏 올려도 통과.
- 정답 분포 경고(각 8~17)는 50문항용 하드코딩이라 80문항이면 **항상** 뜬다.
  `warns` 라서 검증은 통과한다 — 무시하면 된다.
- 낭독본을 화면 해설보다 길게 쓰는 건 `bundle.py` 가 **원래 지원하는 동작**이다
  (404행 주석: "화면 해설은 짧게, 대본(낭독)은 길게 — 표시/낭독 분리는 speech 로만").
- `bdae-written-syllabus.md` 는 플러그인에 이미 포함돼 있다.

> 선택 사항: 정답 분포 경고를 문항 수에 맞춰 자동 조정하려면 `validate.py` 의
> `if any(v > 17 …)` 세 줄을 아래로 바꾼다. 50문항이면 8~17 로 현재와 동일, 80문항이면 13~27.
> ```python
> lo, hi = round(n / 4 * 0.64), round(n / 4 * 1.36)
> if any(v > hi or v < lo for v in ans.values()):
>     warns.append(f"{code}: 정답 분포 편중 {dict(ans)} (각 {lo}~{hi} 권장)")
> ```

## 알아둘 것 두 가지

**개념 풀이 기출 1회차(80문항)뿐이다.** `01/` 에 `01-*` 만 있어서 80문항으로 240문항을 파생한다.
`derived_from` 중복이 회차당 3회씩 생기는 건 정상. 실제 근거는 syllabus 의 60개 개념 + 보충 항목이라
기출 80문항보다 넓다.

**낭독이 길어지면 슬라이드 체류 시간도 길어진다.** `durSec` 는 #3 이 TTS 길이로 확정하므로,
일반영상(`*.static.mp4`)은 정지 이미지가 오래 머문다. 프롬프트의 "▶ 실전 포인트" 한 줄이
화면 전환을 한 번 더 만들어 이를 완화한다. 리모션(키네틱)은 모션이 붙어 덜 문제된다.

## 후속 단계

`_rounds/` 가 비어 있으므로 **m01~m03** 으로 생성된다. 완료 후:

1. XAM LOCAL(`run.bat` → `http://127.0.0.1:8870/`)에서 `#/questions` 문항 교정
2. `#3 chodangi-mp4-forge` 의 `render.bat mNN-K` → 일반영상
3. 클로드 데스크탑 리모션 → `draft/*.motion.mp4`
