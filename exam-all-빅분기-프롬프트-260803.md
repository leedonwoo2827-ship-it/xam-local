# /exam-all 실행 프롬프트 — 빅데이터분석기사 필기 (2026-08-03)

> 클로드 데스크탑에 **아래 코드블록 전체를 그대로 붙여넣기.**
> 플러그인(exambook-forge)은 설치돼 있고 코드는 수정하지 않는다.
> 사전 준비는 playwright 1회 설치뿐(맨 아래).

**이 판이 260730 판과 다른 점** — 세 가지가 바뀌었다.

| | 260730 판 | 이 판 |
|---|---|---|
| 자사 회차 | 3회차 240문항 · 24편 | **9회차 720문항 · 72편** |
| 기출 풀(`01/`) | 1회차 80문항 | **3회차 240문항** — 파생 중복이 없어졌다 |
| 영상 1편 | 약 4분 | **약 15분** — 낭독을 늘리고 화면을 문항당 3~4장으로 쪼갠다 |

`02/ 03/ 04/ 05/ 06/ _rounds/` 는 **전부 비워 두었다.** 백업할 것이 없으니 그대로 새로 만든다.

---

```
/exam-all rounds=9 book=D:\00work\ocr-output-260730 chunk=10 subject=빅데이터분석기사 theme=teal

■ 품목: 빅데이터분석기사 필기 (SQLD 아님). SQL 문항 없음, 통계·알고리즘·평가지표 중심.
  references/bdae-written-syllabus.md 를 개념 풀로 쓰고,
  references/round-data-schema.md 와 authoring-rules.md 의 난이도·분포·과목비율 기준은
  SQLD 50문항용이므로 아래로 전면 대체한다.

■ 회차 구성 — 9회차 (m01 ~ m09)
  회차당 80문항 = 4과목 × 20, 전 문항 4지선다
    subject_no 1  빅데이터 분석 기획   question_no  1~20
    subject_no 2  빅데이터 탐색        question_no 21~40
    subject_no 3  빅데이터 모델링      question_no 41~60
    subject_no 4  빅데이터 결과 해석   question_no 61~80
  subject 문자열은 위 네 개를 한 글자도 바꾸지 말 것
    (요약노트 <h1>N과목 · 이름</h1> 과 일치해야 성적표 링크가 붙는다)
  정답 분포: ①②③④ 각 15~25
  9회차를 서로 다른 하위주제로 차별화하고, 뒤 회차로 갈수록 상 비중을 상단(32)에 맞춘다.

■ ★ 최우선 지시 1 — 난이도를 실전 수준으로
  기출 01/ 의 자체 분포는 실제 시험보다 쉽다. 표면만 재작성하면 쉬운 문제집이 복제된다.
  기출은 "출제 개념의 근거" 로만 쓰고, 난이도는 syllabus 개념을 실전 수준까지 새로 설계한다.
  derived_from 은 추적용이지 난이도 기준이 아니다.

  목표 분포 (합 80): 상 28~32 · 중 42~46 · 하 6~8

  난이도 루브릭
    하 : 단순 정의·분류 암기 1단계          ← 회차당 8문항 이하
    중 : 개념 비교·2단계 추론, 표/그래프 해석, 절차 순서 판단
    상 : 아래 중 하나 이상을 반드시 포함
         (a) 수치 계산 후 판단 — 혼동행렬→정밀도/재현율/F1, 지지도·신뢰도·향상도,
             IQR 이상값 경계, 엔트로피·지니·정보이득, 오즈비, R²·수정R², RMSE/MAE 비교
         (b) 두 개 이상 개념 결합 — 불균형 데이터의 평가지표 선택, 다중공선성 진단 후
             규제 기법 선택, 차원축소 후 군집 평가
         (c) 표·시나리오 지문 해석 — round JSON 의 table 필드를 적극 사용
         (d) ㄱ/ㄴ/ㄷ/ㄹ 조합형 "모두 고른 것은"     ← 회차당 6~10문항

  오답(distractor)은 수험생이 실제로 혼동하는 개념 쌍에서 가져온다. 무의미한 보기 금지.
    정밀도↔재현율 / 민감도↔특이도 / 회귀지표↔분류지표 / 정규화↔표준화 / PCA↔요인분석
    배깅↔부스팅 / L1 Lasso↔L2 Ridge / 지지도↔신뢰도↔향상도 / 1종오류↔2종오류
    과대적합↔과소적합 / KDD↔CRISP-DM↔SEMMA 단계 순서 / 가명처리↔익명처리↔총계처리
  전형적 함정: 불균형 데이터에서 정확도의 함정, AUC 해석, 재현율↔정밀도 트레이드오프,
    상관관계≠인과관계, 표본오차 vs 비표본오차
  부정형("옳지 않은 것") 발문 회차당 15~20문항, 부정어는 굵게.
  계산형은 회차당 20문항 이상, 정답은 반드시 직접 검산해 확정.

■ ★ 최우선 지시 2 — 영상 1편을 약 15분으로 (문항당 낭독 90초)
  번들 1편 = 10문항이다. 15분(900초) 목표이므로 **문항당 낭독 약 90초.**
  260730 판은 문항당 25초여서 4분짜리가 나왔다. 낭독을 3~4배로 늘린다.

  ★ 화면 페이지 분할은 **손으로 하지 않는다 — bundle.py 가 자동으로 한다.**
    (`scripts/bundle.py` 529~562행: playwright 로 1920×1080 에서 블록별 실제 높이를 재고,
     1080 − 상하 패딩 − 푸터 에 맞춰 문단 경계에서 페이지를 나눈다. 발문·정답 배지는
     페이지마다 상단 고정, 이어지는 해설 페이지엔 보기 4개를 반복하지 않는다.)
    낭독도 자동 배정된다 — `_assign_speech()`(682~690행)가 발문 낭독은 1페이지에 통째로,
    해설 낭독은 **표시 분량 비율로 문장을 나눠** 각 페이지에 배정한다.

  → 그러므로 **explanation 을 길게 쓰면 화면 페이지가 자동으로 늘어나고 낭독도 같이 나뉜다.**
    260730 판의 "화면 해설은 1페이지 분량으로" 제약은 이제 걸지 않는다.
    구분선(`▶ …`)을 억지로 넣을 필요도 없다. 문단으로 자연스럽게 나눠 쓰면 된다.

    explanation 을 아래 순서로 쓰되, 각 덩어리를 **문단으로 분리**한다:
      1) 정답 근거 + 핵심 공식/수치
      2) 오답 4개를 하나씩 — 왜 그럴듯한지 → 어디서 어긋나는지
      3) 실전 포인트 — 이 유형이 나오는 방식 · 헷갈리는 개념 쌍 구분 요령 · 암기법
      4) 연결 개념 (필요할 때만)

  ※ 단 하나 반드시 지킬 것 — **playwright 가 설치돼 있어야 한다.**
    없으면 분할 함수가 그냥 `None` 을 돌려주고(532~533행) **분할이 통째로 생략된다.**
    그러면 한 슬라이드에 전부 밀어 넣어 보기 박스가 서로 붙고 내용이 잘린다.
    260730 판에서 실제로 그 증상이 나왔다. 먼저 아래를 돌리고 시작할 것:
      pip install playwright && python -m playwright install chromium

  explanation        : 화면·자막. 문단으로 나눠 쓴다(페이지 분할은 자동).
  explanation_speech : 낭독. 화면의 3~4배. "정답은 N번입니다." 로 시작.
    화면에 없는 내용을 읽어도 된다 — 오답 해부의 상세, 시험장 풀이 순서, 암기 근거,
    비슷한 문항과의 비교는 낭독으로만 가도 된다.
    순수 텍스트만 (마크다운 **·백틱·![]() 금지), 발음 표기로
      RMSE→"알엠에스이"  R²→"결정계수"  F1→"에프원 스코어"  AUC→"에이유씨"
      K-fold→"케이 폴드"  1.2%→"일 점 이 퍼센트"
    괄호 (…) 부가설명은 낭독에서 빼고 화면 자막에만 남긴다.

■ 기출 풀이 3회차로 늘었다 — 파생 중복이 없어졌다
  01/ 에 01-* · 02-* · 03-* **240문항**이 있다(260730 판은 80문항뿐이었다).
  derived_from 은 그 240개 중 실제 문항 id 를 쓴다. 720문항이므로 한 기출당 3회 파생이
  되지만, 개념 풀은 syllabus 60개 개념 + 보충 항목이라 기출보다 넓다.
  같은 기출에서 파생한 3문항은 **서로 다른 회차·다른 하위개념·다른 난이도**로 갈라 놓을 것.

■ 산출물 — 04·05 는 선택이 아니라 필수
  _rounds/  m01.json … m09.json   (집필 원천)
  02/       mNN-01 ~ mNN-80 + _index.json + difficulty_stats.json + assets/*.svg
  03/       요약(이론) HTML+MD — 기출 01/ 3회차 + 자사 m01~m09 전체를 병합해 생성.
            난도가 올라간 만큼 계산 공식(정밀도·재현율·F1·향상도·정보이득·R²)은
            유도 과정까지 실을 것.
            <h1>N과목 · 이름</h1> 의 이름 4개는 위 subject 문자열과 정확히 같게.
  04/       lesson_mNN.json (영상 대본)
  05/       mNN-1 … mNN-8 — 회차당 8편, 9회차 = **총 72편**
            각 편에 source/deck.html + _deck.css/_deck.js + 부분 lesson,
            images/slide_*.png, script/mNN-K_script.json, review.json 스켈레톤
  ※ generate-mockexam SKILL 의 "웹-only 품목은 04/05 생략 가능" 은 적용하지 말 것.

■ 슬라이드 넘침
  playwright 가 있으면 bundle.py 가 알아서 나누므로 보통 넘치지 않는다.
  그래도 "[warn] 슬라이드 N 이 …px 넘칩니다" 가 뜨면 **한 블록 자체가 1080px 을
  넘는다는 뜻**이다(표가 너무 크거나 한 문단이 지나치게 길다).
  deck.html 을 손대지 말고 그 블록을 문단으로 쪼개거나 표를 줄인 뒤 다시 돌린다.
  낭독(explanation_speech)은 줄이지 않는다 — 슬라이드 높이와 무관하다.

■ 집필 품질
  SVG 를 아낌없이 — 혼동행렬, ROC 곡선, 편향-분산, 덴드로그램, CRISP-DM 단계,
    박스플롯/IQR 경계, K-fold 분할, 마진/커널, 지니 vs 엔트로피 곡선
  tags 2~4개. 지문이 필요한 문항은 round JSON 의 table / sql / passage 필드를 쓴다.

■ 진행
  validate.py 오류 0 확인 → build.py → bundle.py 순서
  ※ validate.py 의 "정답 분포 편중 (각 8~17 권장)" 경고는 50문항용 하드코딩이라
    80문항이면 항상 뜬다. warns 라서 검증은 통과한다 — 무시하고 진행.
  회차 단위로 끊어서 보고 (m01 완료 → m02 → … → m09)
  각 회차 보고에 난이도 분포 + "상 문항 중 계산형 몇 개 / 조합형 몇 개" +
    **번들 1편의 예상 길이(초)** 를 밝힐 것. 15분(900초±120)에서 벗어나면 알려 줄 것.
  마지막에 회차별 난이도·정답 분포, 번들 편수(72), 산출 경로를 표로 정리.
```

---

## 사전 준비 (최초 1회)

```
pip install playwright && python -m playwright install chromium
```

플러그인 수정이 아니라 파이썬 패키지 설치다. 없으면 `bundle.py` 가 슬라이드 페이지 분할을
건너뛰고 경고만 남겨 **슬라이드가 잘린 채로 나온다.**

## 왜 이 인자들인가 — 기본값이 전부 SQLD다

| 항목 | 플러그인 기본(SQLD) | 이 프롬프트 |
|---|---|---|
| `subject` / `theme` | SQLD / sqld | 빅데이터분석기사 / **teal** (`#0D9488`) |
| `rounds` | — | **9** (m01~m09) |
| 회차당 문항 | 50 (2과목 10:40) | **80** (4과목 × 20) |
| 난이도 분포 | 상 12~16 / 중 24~28 / 하 8~10 | **상 28~32 / 중 42~46 / 하 6~8** |
| 정답 ①②③④ 각 | 8~17 | **15~25** |
| `chunk=10` 결과 | 회차당 5편 | **회차당 8편** → 9회차 **72편** |

## 플러그인은 건드리지 않는다

- 난이도 분포는 `validate.py` 의 검증 대상이 **아니다**(출력만 한다). 올려도 통과한다.
- 정답 분포 경고(각 8~17)는 50문항용 하드코딩이라 80문항이면 항상 뜬다. `warns` 다.
- 낭독을 화면 해설보다 길게 쓰는 것은 `bundle.py` 가 **원래 지원하는 동작**이다
  (화면 해설은 짧게, 낭독은 길게 — 표시/낭독 분리는 speech 로만).
- `bdae-written-syllabus.md` 는 플러그인에 이미 포함돼 있다.

## XAM LOCAL 쪽이 기대하는 형식 (어긋나면 저장이 막힌다)

XAM LOCAL 은 `_rounds` · `02/*.md` · `05/lesson` 세 파일을 **바이트 단위로 왕복 검증**한다.
통과하지 못하면 문항 저장이 409 로 막힌다. 아래만 지키면 된다.

- **`02/*.md` 의 `## 지문`** 은 round JSON 의 `passage` · `table` · `sql` 에서 나온다.
  `table` 은 `{"columns": [...], "rows": [[...]]}` 형태. `null` 셀은 문자열 `None` 으로 나간다.
- **`05/lesson` 의 problem 블록** 키 순서:
  `kind number type question [passage] choices answer answer_index
   explanation explanation_speech difficulty tags [assets]`
  `passage` 는 **지문이 있는 문항에만** 넣고, 값은 인라인 그림 줄을 **뺀** 형태다
  (그림은 `assets` 가 들고 있어 중복이다). 지문이 없으면 키 자체를 넣지 않는다.
- **`assets`** 는 `05/lesson` 에서 파일명 배열(`["m01-02-dmz.svg"]`), `_rounds` 에서는 인라인 SVG.
- 개행·indent 는 **자유다.** XAM LOCAL 이 파일에서 되맞춘다(CRLF/LF, indent 1/2, 인라인 배열).
- `_index.json` 의 `round` · `subject_no` 는 **정수**여야 한다. 문자열이면 웹의 과목 필터가 깨진다.

## 완료 후

1. XAM LOCAL 을 띄우고(`run.bat` → `http://127.0.0.1:8870/`)
   `#/questions` 에서 문항 교정. `Ctrl+Enter` = 저장 + 검수완료 + 다음 미검수로.
2. `#/video` 에서 번들 사전점검 → 렌더.
   **`render.bat` 은 쓰지 않는다** — 렌더 엔진이 XAM LOCAL 안(`vendor/chodangi`)으로
   들어왔고, 자막 시간축 수정 4건도 그 소스에 반영돼 있다.
3. `#/publish` 에서 사전점검 → 빌드 → FTP 목록 → 서버 체크리스트.

> 자막 시간축은 2026-08-03 에 맞췄다. `review.json` 에 `timebase: "video"` ·
> `crossfadeSec: 0.6` 표식이 붙으며, `startSec` 을 그대로 시크에 쓸 수 있다.
> 표식이 없는 번들은 예전 것이다(읽는 쪽이 보정한다).
